# ROYAL11 — Product Requirements & Build Log

## Original Problem Statement
Build a modern, clean, full-width responsive web app UI for "ROYAL11" — an all-in-one sports,
fantasy sports, and casual gaming entertainment platform using virtual (non-cash, non-withdrawable)
coins. Bright, friendly, trustworthy sports-app aesthetic (Dream11/FanCode energy), NOT a dark casino
look. Two core screens (Home, Wallet) + a bottom nav (Home, Sports, Fantasy, Games, Wallet).

## Architecture
- **Frontend**: React 19 + Tailwind, framer-motion (motion/reveals), Lenis (smooth scroll),
  lucide-react icons, sonner toasts, react-router. Fonts: Outfit (display) + Manrope (body).
  Palette: Royal Indigo #4F46E5 + Flame Orange #F97316 on soft-neutral #F5F6FB.
- **State**: In-memory shared wallet via React Context (`WalletContext`) — balance, transactions,
  owned items, equipped avatar, boost timer. Resets on refresh (frontend-only by user choice).
- **Backend**: FastAPI + MongoDB (motor). One AI endpoint so far.
- **AI**: Claude Sonnet 4.6 via `emergentintegrations` + `EMERGENT_LLM_KEY`.
  `POST /api/fantasy/coach` — Claude ranks players; backend enforces the credit budget (hybrid),
  with a safe fallback. Suggestions logged to `ai_suggestions` collection.

## User Persona
Casual sports/fantasy gamer who wants a fun, trustworthy hub to follow live matches, build fantasy
teams, play casual games, and manage virtual coins — no real-money gambling.

## Core Requirements (static)
- Home: greeting header + bell + avatar; coin balance card (+today, View Wallet / Earn Coins);
  quick actions; Live Now cards; fantasy promo banner; games lobby (6 tiles); daily streak strip.
- Wallet: back + title; big balance + virtual-coins disclaimer; 3 stat tiles; How to Earn / Redeem;
  transaction history.
- Bottom nav (Home + Wallet functional; Sports/Fantasy/Games placeholders).

- 2026-08-16 (fork): **Opponent-seat visibility fix (Rummy).** User reported the opponent seat looked empty/broken (bare abstract avatar, no name, no balance) — "opponent isn't even showing." Root cause: the AAA redesign had removed seat name labels, and the coin pill (`coinText`, `RummyTable.jsx` ~line 490) only renders for `p.is_you`; opponents got nothing. NOT a regression from this session's felt/pile CSS. Fix: added a **name label under every seat** (`rummy-seat-name-{uid}` → shows `display_name`, "You" for self, gold when it's their turn). Opponent wallet balances are intentionally NOT shown (server doesn't expose other players' balances — privacy/fairness); name + avatar + active-ring + turn pill make the opponent clearly present. Verified live at 1920px: seats show "Casino P2" and "You".
## Implemented (with dates)
- 2026-08-16 (fork): **Figma redesign queue — Phases A/B/C (frontend-only, cosmetic).**
  - Phase A live-gameplay fixes: desktop felt max-width cap so the oval keeps Figma proportions on wide screens (`body:not(.rummy-ls) .vegas-felt--aaa` max-width 440px ≥768 / 480px ≥1280 → ~1.6:1 rounded compact oval, tightened iteratively from 700/760 → 600/640 → 520/560 → 440/480 after repeated user feedback that it read stretched; `casino-vegas.css`); draw/discard/joker pile cards now use the premium `rich` deck (`RummyTable.jsx`); Deals-match funds UX — an OPPONENT shortfall now shows a neutral toast instead of pushing the current player to the "pay your agent" recharge modal (`doStart` catch, only recharge when `lowBalance`).
  - Phase B: MY TABLE (`MyTable.jsx`) gained the header ROYAL11 wordmark + real-wallet coin-balance pill (`mytable-coin-balance`) + green add-funds button (`mytable-add-coins`, opens AddCoins) + VIP-room heading, per R11-TBL. Refer & Earn modal (`ReferAndEarn.jsx`) and referral **flat-200** (`referral_service` `referrer_amount`=200 in DEFAULTS + DB) were already implemented (title shows 200, code+share, promo field+Apply, virtual-coin disclaimer). Daily Bonus widget already present.
  - Phase C: **Lucky 777 Host Wardrobe** — new `components/HostWardrobe.jsx` full-screen landscape panel + `lib/hostWardrobe.js`. 11 original AI-generated outfit illustrations (consistent host character "Zoya") + "No Host": Royal Sari, Marathi Nauvari, Gujarati Sari, South Indian Silk, Punjabi, Bengali, Indian Lehenga, Nagaland, Vegas Dress, Luxury Abaya, Air Hostess. ALL/INDIAN/MODERN filters, preview stage + Host Info, Preview/Apply. Purely cosmetic (persisted `royal11_slots_host_outfit`); wired into `SlotsPage.jsx` via a wardrobe button (`slots-host-btn`) and the applied host renders on the slots stage (`slots-host-portrait`). No RNG/ledger/API changes. Verified via screenshots (desktop wide felt, MY TABLE, wardrobe filter+apply+host-on-stage).
  - STILL OPEN (deferred, user-deprioritized): Deals matchmaking should prevent seating an opponent who can't cover the entry fee (backend) OR top up demo/bot balances — currently only the frontend UX was fixed.
- 2026-08-15 (fork): **Rummy mobile-landscape rebuild + PWA + true Fullscreen** — Rebuilt the immersive Rummy table for phones as a fixed single-screen LANDSCAPE layout (flex column, no page scroll). Root-caused & fixed the P0 where the old "rotate a tall scrolling column" hack pushed the hand tray + 6-button action bar off-screen. Now: thin single-row header, capped elliptical table, sticky one-row 6-button action bar (Draw/Discard/Sort/Group/**Drop**/Declare all always visible), Daily Bonus is reminder-only (hidden except final 10s / when claimable). Added PWA (`manifest.json` display:standalone + Apple meta tags + `InstallPrompt` banner) and vendor-prefixed Fullscreen for chrome-free play. Verified via Playwright DOM measurements + screenshots at 390×844 (iOS rotate) and 844×390 (Android landscape). Details in test_credentials.md.
- 2026-06: MVP Home + Wallet + bottom nav (verified 100%).
- 2026-06: Team Builder (draft XI, 100-credit budget), Rewards Store (avatars/badges/boosts),
  Live Score Ticker.
- 2026-06: Captain/Vice-Captain picks (2x/1.5x); live Contest Leaderboard (rank + points tick);
  Equip Rewards (avatars + badges shown on Home).
- 2026-06: Reward Wheel (Lucky Spin), 2x Boost effects + countdown, Boost Extend, Boost Tiers
  (5-min Mega boost, stacking).
- 2026-06: Match Detail scorecard with live ball-by-ball commentary (cricket + football).
- 2026-06: **Fantasy AI Coach** (Claude Sonnet 4.6) — "AI Coach" button auto-picks XI + C/VC +
  rationale; backend enforces budget. Verified 100% (backend pytest + frontend flow).

- 2026-06: **Server-authoritative backend** — JWT auth (`/api/auth/*`), coin-ledger wallet with idempotency (`/api/wallet/me`), and SUPER_ADMIN→MANAGER→ADMIN→PLAYER hierarchy (`/api/admin/*`) under `backend/app/` + `seed.py`. Verified 100% (32/32 pytest). Note: seed emails use `@royal11.com` (email-validator rejects `.local`).

- 2026-07: **Frontend wired to real backend (Auth + Wallet)** — `AuthContext` (JWT Bearer, `localStorage['royal11_token']`, `/api/auth/me` on mount, login/register/logout, `formatApiErrorDetail`), `AuthPage` (Log In / Sign Up, matches Cherry-Red theme), `ProtectedShell`/`AuthRoute` in `App.js` (unauth → `/auth`), and `WalletContext` now reads real balance + history from `GET /api/wallet/me` (never locally mutated). Logout button on Home; greeting uses real `display_name`. Per product decision, coin-spending actions without a ledger endpoint yet (Earn Coins, Redeem/buy, Lucky Spin, Boost extend, Contest join/lock, Streak claim) are **disabled** and show a "Coming soon" toast — no fake balance changes. Verified 100% (9/9 e2e flows, iteration_8).
- 2026-07: **AI Match Preview (Gemini 3 Flash)** — `MatchDetail` modal fetches `POST /api/match/preview` on open, rendering an AI preview + favorite/win-probability bar + prediction (with graceful fallback). Verified live.
- 2026-07: **New R11 crest logo + video intro splash (visual-only)** — Replaced the header emblem with the Figma "R11" crest (`src/assets/royal11-logo*.png`, re-hosted locally) plus a thin gold accent bar; updated favicon/app-icons + title in `public/`. Added `SplashScreen.jsx` that plays a full-screen portrait intro video (`public/royal11-intro.mp4`, ~10s, H.264/AAC, autoplay+muted+playsInline, no controls) once on load, with a top-right **Skip** button, a top-left **mute/unmute** toggle (`VolumeX`↔`Volume2`, starts muted), `onEnded` handoff, and an 11s safety timer. On decode/load failure (e.g. browsers without H.264), it gracefully falls back to a branded crest card (~2.3s) then hands off. Handoff goes to Home (valid token) or Login. No backend changes.
- 2026-07: **Favorite IPL team personalization (visual-only)** — `lib/iplTeams.js` (10 teams: colors + slogans, `useFavoriteTeam` localStorage hook `royal11_fav_team`, luminance-based readable-text helper). `TeamBuilder.jsx` shows a first-run "Choose your team" picker (badge grid), then themes the Fantasy screen with that team's accent (gradient header, slogan banner, picked-card border/tint, pick-toggle + Lock CTA colors) and a "change team" chip to re-pick. Persists across reload. Verified live (MI + RCB). No backend/wallet changes.
- 2026-07: **Super Admin API Keys management (new feature)** — First Super Admin dashboard screen. Backend (`app/crypto_utils.py` Fernet encrypt-at-rest keyed by new `APP_ENCRYPTION_KEY` env, `app/api_keys_service.py` provider auto-detect + cheap live "list models" tests via httpx, `app/routers/api_keys.py`) exposes `POST/GET/DELETE /api/admin/api-keys`, `POST /api/admin/api-keys/test` (ad-hoc pre-save), `POST /api/admin/api-keys/{id}/test` — all behind `require_roles(SUPER_ADMIN)` and audit-logged. Keys stored encrypted; only provider + last4 ever returned; full key never logged/returned. `balance_info` honestly null (no provider exposes it via a simple key call). Frontend: `/admin` SUPER_ADMIN-only route (others redirected), admin nav shield on Home, `pages/AdminPage.jsx` with add form (debounced provider auto-detect, test-before-save, save), saved-keys list with status dots (green=ok+recent / yellow=untested·stale / red=failed), masked key, balance cell, per-row test + delete. Verified live end-to-end (add/test/save/re-test/delete + player redirect + 403 guard). New env vars added: `APP_ENCRYPTION_KEY`, `SPORTMONKS_CRICKET_API_KEY`.
- 2026-07: **Real cricket data via Sportmonks (server-side)** — `app/cricket_service.py` calls Sportmonks Cricket v2.0 (`livescores`, `fixtures`) using `SPORTMONKS_CRICKET_API_KEY` from env (**never** exposed to frontend), normalizes to the UI's match shape, and caches in-memory 45s. `app/routers/cricket.py` exposes public `GET /api/cricket/live` and `GET /api/cricket/matches`. Errors are handled gracefully (returns `{status:"unavailable"}`; never logs the token — only exception type). Frontend HomePage "Live Now" → renamed **Live Cricket**, now fetches `/api/cricket/live` (polls 45s) with four states: loading, list (`LiveCard`), empty ("No live matches right now"), and error ("Live scores temporarily unavailable"). Removed the old mock `LIVE_MATCHES` ticker + fake score/goal simulation. Verified live (empty + forced-error states). NOTE: `MatchDetail` still runs an animated ball-by-ball commentary as a visual flourish (only reachable from a live card; its AI preview is real) — not yet wired to real ball data.

- 2026-07: **Role-based Admin Console — Phase 1 (`/console`, NEW dark desktop dashboard)** — Replaces the old `/admin` page (deleted). A professional dark Admin Platform (page `#0d0d0d`, sidebar `#090607`, cards `#1b1012`, gold `#d4af37` accents, cherry `#c41230` primary), Outfit font, 240px grouped sidebar (CORE / COINS & FINANCIALS / SYSTEM), top search bar. Role-aware: **Super Admin** → Overview (real stat cards from `GET /api/admin/overview` + Manager Allocation & Performance table; honest "Coming soon" cards for DAU/alerts/time-series we don't track yet), Managers (list + create + fund + set-quota), Admins (read-only, with allocated/used/remaining/usage), Transactions (paginated `GET /api/admin/transactions` with player→admin→manager chain, type filter, reverse-with-confirm), API Keys (restyled dark). **Manager** → My Admins (quota stat cards + create admin + allocate coins) + Transaction History. **Admin** → My Players (grant coins) + Transaction History. **Player** → redirected to Home; `/console` guarded by `CONSOLE_ROLES`; admin roles redirect to `/console` after login. Backend additive endpoints in `admin.py`: `GET /admin/overview`, enriched `GET /admin/managers` (admin_count/usage), enriched `GET /admin/admins` + `GET /admin/my-admins` (allocated/used/usage/player_count via `_admin_flows` from the ledger), paginated `GET /admin/transactions` (`{items,total,skip,limit}` + `type` filter). Frontend under `src/console/*` (primitives, api hook, 7 panels) + `pages/ConsolePage.jsx`. Verified 100% — backend 21/21 pytest (`tests/test_console_admin.py`), all frontend role flows + role isolation (iteration_9). No fake data anywhere.

- 2026-07: **Coin top-up + revenue-share + settlement system (Parts 1–5)** — All fully backend-verified + frontend-tested (iteration_10 Parts 1–4, iteration_11 Part 5 + re-theme; pytest `tests/test_console_admin.py`, `test_royal11_recharge_part5.py`).
  - **P1 Deposits (manual)**: `deposits` collection; player `POST /api/wallet/deposit-request` (PENDING, no auto-credit), `GET /api/wallet/deposit-info` (assigned Admin + bank + 1 INR:1 coin ratio), `GET /api/wallet/deposits`; Admin `POST /api/admin/deposits/{id}/confirm` (idempotent credit `deposit:{id}`) / `/reject`, scoped `GET /api/admin/deposits`. UI: player `AddCoins.jsx` (Wallet → Add Coins), Console Deposits tab (chat-thread confirm/reject).
  - **P1b Bank accounts**: `admin_bank_accounts`; `GET/PUT /api/admin/bank-account` (Admin/Manager). Console Bank Account panel.
  - **P2 Revenue split + weekly settlements**: `revenue_split_super_admin_pct` per-admin (default 70), `PATCH /api/admin/admins/{id}/revenue-split`; `settlements` collection (Sun–Sat, due following Wed), idempotent lazy+scheduled generation, `GET /api/admin/settlements`, `POST .../{id}/settle`. Console Settlements tab + Admins "Split" action.
  - **P3 Usage alerts + auto-suspend**: `UserStatus.SUSPENDED`; 100%-used allocation auto-suspends Admin (COINS_EXHAUSTED) via `sync_admin_usage_suspension` on grant; re-allocation reinstates; suspended = read-only Console + action endpoints 403 (`require_not_suspended`). Usage badges + suspended banner.
  - **P4 Referral + nudge + daily summary**: player `referral_code`/`referred_by`, referrer +200 coins on signup (idempotent `referral:{new_user}`), `/me` backfills code; `POST /api/auth/activity` returns a non-punitive 2-day inactivity nudge; `daily_summaries` + `GET /api/admin/daily-summary(/export)` CSV. UI: signup referral field, Wallet referral card, Overview Daily Activity view.
  - **P5 Admin self-recharge**: `admin_recharges`; Admin `POST /api/admin/recharge-request` (allowed even while suspended), `GET /api/admin/my-recharges` + `/recharge-info`; Super Admin `GET /api/superadmin/recharges`, `POST .../{id}/confirm` (credits at 1.5x, idempotent `admin_recharge:{id}`, counts as allocation so it lifts COINS_EXHAUSTED) / `/reject`. Console Recharge Quota (Admin) + Recharge Requests (Super Admin) panels.
  - Scheduler: APScheduler daily 00:10 UTC `_daily_maintenance` (yesterday's summary + last week's settlements + overdue-suspension sweep).
- 2026-07: **Console visual RE-THEME (visual-only, all testids preserved)** — Dedicated Console login `/console/login` (orange→red gradient bank-marketing layout: gradient header, headline, icon quick-access cards, sign-in form; `ConsoleRoute` redirects logged-out to it, players → `/`). All other Console screens re-skinned dark→**white + sky-blue banking dashboard** (light sidebar, sky active highlight, IBM Plex Sans body, sky pill buttons, white cards/soft shadows, green/amber/red for status only) via rewritten `console/primitives.jsx` + bulk inline-class conversion. Overview gained a two-option toggle (Coin Economy / Daily Activity). Verified 100% across all 3 roles (iteration_11).

- 2026-08: **Task 1 — Deposit Screenshot Upload + Google Cloud Vision OCR + duplicate-UTR fraud flag (DONE, verified iteration_12, 100%)**. 
  - **Storage**: `app/storage_service.py` — Emergent object storage (init at startup, async put/get via `asyncio.to_thread`, retry-on-404 force re-init). Screenshots stored at `royal11/deposits/{player_id}/{uuid}.{ext}`; served through backend only (`GET /api/admin/deposits/{id}/screenshot`, scope-checked, streamed as blob to the console).
  - **OCR**: `app/ocr_service.py` — `GoogleVisionOCR` behind a single `get_ocr_provider()` boundary (swap engine = one-file change). Uses Cloud Vision `images:annotate` DOCUMENT_TEXT_DETECTION via `x-goog-api-key` header + `GOOGLE_CLOUD_VISION_API_KEY` env. **Graceful degrade**: key unset → `status="unavailable"`, verdict `"unknown"` (same pattern as Sportmonks). `parse_and_match()` extracts amount (₹/Rs/INR), UTR (case/punctuation-insensitive alnum substring), and timestamp (advisory). Verdict: green **"Matches"** = amount AND UTR match; else red **"Review carefully"**; timestamp advisory-only (never flips red). OCR NEVER auto-confirms/rejects.
  - **Fraud**: `deposit_service.create_deposit_request` sets `duplicate_utr=true` when reference_note exactly matches a previously CONFIRMED deposit; shows amber banner in console.
  - **API change**: `POST /api/wallet/deposit-request` is now **multipart/form-data** (`amount_inr`, `reference_note`, optional `screenshot` file; 8 MB + image/* validation).
  - **UI**: player `AddCoins.jsx` — optional screenshot attach + preview + FormData submit. Console `DepositsPanel.jsx` — side-by-side "Payment proof" thumbnail (fetch-as-blob, click to zoom) + "Auto-verification" OcrPanel + duplicate banner.
  - **PENDING (user action)**: ✅ RESOLVED 2026-08 — `GOOGLE_CLOUD_VISION_API_KEY` added to `backend/.env`; **live OCR verified end-to-end** (real screenshot → green "Matches" with amount+UTR+timestamp all correct; mismatch → red "Review carefully"). Regression pytest: `tests/test_ocr_service.py`, `tests/test_deposit_screenshot_ocr.py`.

- 2026-08: **Task 2 — Zonal Manager tier + Admin-creation approval + per-Manager admin cap (DONE, verified iteration_13, 23/23 backend + all 3 console roles)**.
  - New role `ZONAL_MANAGER` (SA → ZM → Manager → Admin → Player). `app/hierarchy_service.py` + `app/routers/zonal.py`.
  - **Funding chain**: SA mints into ZM wallet (`SUPER_ADMIN_TO_ZONAL`) + sets ZM `authorized_quota`; ZM transfers to Managers in its zone (`ZONAL_TO_MANAGER`, atomic quota reservation like Manager→Admin). Managers with `zonal_manager_id=null` still funded directly by SA (backward-compatible).
  - **Approval workflow**: Managers can NO LONGER direct-create Admins (`POST /admin/admins` is SA-only). They submit `admin_creation_requests` (PENDING, password stored hashed) → approved/rejected by their ZM (or SA if no zone). Idempotent account creation on approve. Console "Admin Requests" queue (SA/ZM approve; Manager read-only).
  - **Per-Manager cap** `max_admins_allowed` (null=unlimited) set by SA or owning ZM; existing admins + pending requests both count; hard-block at cap on submit + re-check on approve. `PATCH /admin/managers/{id}/max-admins`.
  - Endpoints: `POST/GET /admin/zonal-managers`, `PATCH .../{id}/quota`, `POST .../{id}/fund`, `GET /admin/zonal/my-allocation|my-managers`, `POST /admin/zonal/managers|fund-manager`, `POST /admin/admin-requests` (+`/approve`,`/reject`), `GET /admin/admin-requests`. Console: `ZonalManagersPanel` (SA), `MyManagersPanel` (ZM), `AdminRequestsPanel` (SA/ZM/Manager); `ManagersPanel` gained Zone+Cap columns + Cap action + optional zone on create. Seed adds `zonal1@royal11.com` (funded); `manager1` stays no-zone. `CONSOLE_ROLES` now includes ZONAL_MANAGER.

- 2026-08: **Multi-bank accounts + UPI ID + auto QR (DONE, verified iteration_14, 10/10 backend + all frontend)**.
  - Admin/Manager can hold MULTIPLE `admin_bank_accounts`; exactly one `is_active` (activating one deactivates the rest; old accounts kept, never deleted). Migration drops legacy unique(admin_id) index + backfills `id`/`is_active`.
  - Each account has optional `upi_id`; scannable **UPI QR** generated client-side (`qrcode.react`) from `upi://pay?pa=<upi_id>&pn=<name>&cu=INR` (`frontend/src/lib/upi.js`). No photo upload.
  - Deposits auto-stamp `account_id` = active account at creation. Per-account CONFIRMED-deposit totals (this-week Sun–Sat + all-time) via aggregation.
  - Endpoints: `GET/POST /admin/bank-accounts`, `PATCH /admin/bank-accounts/{id}/activate` (old singular `/bank-account` removed). UI: rewritten Console `BankAccountPanel` (cards + totals + active Switch + add modal w/ live QR preview) + player `AddCoins` (active account QR + details side-by-side). Regression: `tests/test_multi_bank_upi.py`.
  - **Add-on (2026-08)**: optional bank-account **nickname/label** (e.g. "Primary GPay") — `BankAccountInput.label`; shown on the account card headline.

- 2026-08: **Task 3 — Salary + Incentive payroll for Managers & Zonal Managers (DONE, verified iteration_15, 18/18 backend + all frontend)**. `app/payroll_service.py`.
  - **SALARY** = fixed guaranteed weekly amount (`weekly_salary_inr`, default 0), paid every settlement week (Sun–Sat) regardless of performance. **INCENTIVE** = extra bonus only when downline CONFIRMED deposit revenue that week ≥ `incentive_target_inr`: bonus = round(revenue × `incentive_pct`/100). Both funded from the Super Admin share (Admin's 30% untouched), credited 1:1 as coins, idempotent (`salary:{uid}:{ws}`, `incentive:{uid}:{ws}`), TxnType `SALARY`/`INCENTIVE`. Manager downline = admins under them; ZM downline = all admins under all managers in the zone.
  - Evaluated in the APScheduler `_daily_maintenance` via `run_recent_payroll` (pays the most recent completed week, idempotent). Live projection endpoints `GET /admin/my-payroll` (Manager) + `GET /admin/zonal/my-payroll` (ZM). SA sets via `PATCH /admin/managers/{id}/payroll` + `PATCH /admin/zonal-managers/{id}/payroll`.
  - UI: SA "Pay" action + modal on `ManagersPanel` & `ZonalManagersPanel`; `PayrollCard` (salary + incentive + total + progress bar) on Manager (`MyAdminsPanel`) & ZM (`MyManagersPanel`) consoles. Regression: `tests/test_payroll_whatsapp_banklabel.py`.

- 2026-08: **WhatsApp contact + wa.me deep-link (DONE, verified iteration_15)**. Admin/Manager set `whatsapp_number` (with country code) via `PUT /admin/profile/whatsapp` (`GET /admin/profile`), edited in the Console Bank Account tab (`whatsapp-card`). Players get a "Chat on WhatsApp" button (`https://wa.me/<digits>?text=...`, `frontend/src/lib/whatsapp.js`) in two places: (1) player `AddCoins` near the bank/QR section (prefill "Hi, I need help with my payment"; `deposit-info`/`my-agent` expose `admin_whatsapp`), and (2) a one-time post-signup `WelcomeAgentModal` (flag `r11_welcome`) naming the assigned admin (prefill "Hi, I just signed up on ROYAL11"). Button hidden gracefully when no number is set. No Meta Business API — pure device deep-link.

- 2026-08: **Real Coin Actions — game mechanics wired to the real ledger (DONE, verified iterations 16)**. `app/game_service.py` + `app/routers/games.py`. Replaces the old front-end-only/mocked Rewards Store, Lucky Spin, and Boosts with real coin debits/credits through `wallet_service.process_transaction` (idempotent via `request_id`, never client-authoritative).
  - **Rewards Store**: `GET /api/games/store` (catalog of avatars/badges/boosts), `POST /api/games/buy` (debits coins, records owned item; idempotent `store_buy:{user}:{item}` / per-purchase id). Duplicate/insufficient-funds handled gracefully.
  - **Lucky Spin**: `POST /api/games/spin` — server picks the reward server-side (weighted), debits the spin entry fee then credits the won reward, both idempotent (`spin:{...}`). No client-supplied outcomes.
  - **Boosts**: `POST /api/games/buy` (boost items) activates a timed 2x/mega boost stored server-side; UI reflects owned/equipped state.
  - Frontend: HomePage/Wallet/Games surfaces now call these endpoints (removed the "Coming soon" toasts for these actions) and refresh the real wallet balance after each action.

- 2026-08: **QR Amount Prefill for deposits (DONE, verified iteration 16)**. Player `AddCoins` now prefills the UPI QR (`upi://pay?...&am=<amount>`) with the amount the player entered, so scanning opens the UPI app with the exact amount pre-populated (`frontend/src/lib/upi.js` extended with an optional `amount` param). Reduces wrong-amount deposits.

- 2026-08: **Payroll Payslips + Missed-Week Catch-up (DONE, verified iteration 16)**. Extends the Salary + Incentive payroll (`app/payroll_service.py`).
  - **Payslips**: each paid week produces a payslip record (salary, incentive, revenue basis, week range, paid-at). Managers & Zonal Managers view their history via `GET /admin/my-payslips` (Manager) / `GET /admin/zonal/my-payslips` (ZM); Console shows a payslip list on the payroll card.
  - **Missed-Week Catch-up**: the APScheduler `_daily_maintenance` job now back-fills ANY unpaid completed weeks (not just the most recent), so a downtime window never skips a payroll. Idempotent per `(uid, week)` via existing `salary:{uid}:{ws}` / `incentive:{uid}:{ws}` request_ids.

- 2026-08: **Dream11-style Fantasy Cricket Contest System — Phase 1 (DONE, agent-verified iteration_17; USER VERIFICATION PENDING)**. Server-authoritative fantasy on real Sportmonks fixtures. `app/fantasy_service.py` (app logic) + `app/cricket_service.py` (data fetching) + `app/routers/fantasy.py`; `tests/test_fantasy.py`.
  - **Team-builder rules**: 11 players, 100-credit budget (default 8.5 cr/player, Super-Admin-editable per player), max 7 from one real team, role ranges WK 1-4 / BAT 3-6 / AR 1-4 / BOWL 3-6, one Captain (2x) + one Vice-Captain (1.5x).
  - **Scoring** (Super-Admin editable via `PUT /admin/fantasy/scoring-config`): run=1, four_bonus=1, six_bonus=2, fifty_bonus=8, century_bonus=16, duck_penalty=-2, wicket=25, 3-wkt=4, 5-wkt=16, maiden=12, catch=8, stumping=12, run_out=8.
  - **Player pool** built from the real Sportmonks fixture **lineup** (`get_fixture_lineup`); if the squad isn't published yet the pool is empty and contest creation returns a graceful **400** ("Squad/lineup isn't available for this fixture yet") — never a 500 (iteration_17 fix: `cricket_service._fetch_one` catches `httpx.HTTPError` → `{}`).
  - **Contests**: Admin/Super Admin create on a real fixture ID (`POST /admin/fantasy/contests`; entry fee, max players, prize pool, default split 50/30/20). Players browse matches → contests → build XI → join (`POST /api/fantasy/contests/{id}/join`, entry fee debited idempotently, one team per user per contest). `GET /api/fantasy/{matches,contests,my-contests,fixtures/{id}/players}`.
  - **Settlement**: server-side only, from real final Sportmonks batting/bowling stats. Auto-runs ~30 min after a match reaches a FINISHED status via APScheduler, retrying every 15 min for up to 6h if stats are incomplete (then flags `NEEDS_REVIEW`; never pays on partial data). Super Admin can also manually `POST /admin/fantasy/contests/{id}/settle` (409 if not ready) or `/cancel` (refunds all entries). Payouts idempotent (`contest_payout:{id}:{user}`).
  - **RBAC**: create/list = SUPER_ADMIN + ADMIN; settle/cancel/scoring-config/player-credit = SUPER_ADMIN only. UI: Console `FantasyPanel.jsx` (create + list; settle/cancel visible to SA only), player `FantasyPage.jsx` (Matches/My Contests tabs + full-screen team builder), Fantasy entry in bottom nav.



- 2026-08: **Per-Admin Branded Login Page (DONE, verified iteration_18, 100% + pytest test_branding.py 7/7)**. Display-only branding layer over the standard login. `app/branding_service.py` + `app/routers/branding.py`.
  - Fields on the Admin's user doc: `brand_name`, `brand_slug` (auto-generated, unique), `brand_logo_path` (Emergent object storage). Logo ≤4 MB, image only.
  - Endpoints: Admin self `GET/PUT /api/admin/branding`, `POST /api/admin/branding/logo`; Super Admin `GET/PUT /api/admin/admins/{id}/branding`, `POST /api/admin/admins/{id}/branding/logo`; **public (no auth)** `GET /api/public/branding/{slug}` + `GET /api/public/branding/{slug}/logo` (only for ACTIVE admins with branding set; unknown/inactive → 404).
  - Frontend: `AuthPage` accepts optional `branding` prop (BrandedHeader shows logo + "Welcome to <brand_name>"); `App.js` `BrandedLoginRoute` at `/login/:slug` fetches public branding and **falls back to `/auth`** on invalid slug / no branding / inactive admin. Console: Admin `BrandingPanel` (nav "Login Branding", shareable link card + copy/open + logo upload) and Super Admin per-Admin "Brand" action + modal in `AdminsPanel` (reuses `BrandingForm`). Login itself unchanged — same auth/accounts. RBAC enforced (player 403; admin 403 on SA endpoints).

- 2026-08: **Animated "live" dashboard numbers (DONE, verified iteration_18)**. `console/primitives.jsx` `AnimatedNumber` (requestAnimationFrame count-up/down, easeOutCubic ~0.9s, proper cleanup). Wired into Super Admin `OverviewPanel` stat cards (managers/admins/players/circulation/allocated/remaining) and Admin `MyPlayersPanel` stat cards (wallet balance, player count). Triggers on the existing data-refresh cycle; purely visual.

- 2026-08: **CRITICAL crash fix — "destroy is not a function" on startup (DONE, verified iteration_18)**. Root cause: `SplashScreen` unmounted via framer-motion `AnimatePresence` `exit` on React 19 + framer-motion 11.18.0, whose internal effect cleanup threw during `commitHookPassiveUnmountEffects`, blocking the splash→app handoff so NO page loaded. Fix: `SplashScreen` root is now a plain `<div>` with a CSS opacity fade (state `leaving`), and `App.js` renders `{showSplash && <SplashScreen/>}` directly (removed the `AnimatePresence` wrapper). No other framer usage changed (modals unmount immediately without AnimatePresence, so they were never affected).

- 2026-08: **Points Rummy — Phase 1 (DONE, verified iteration_21; backend 16/16 pytest + frontend 2-session e2e)**. Server-authoritative 13-card Indian Points Rummy on the provably-fair 2-deck shoe. `app/games/rummy.py` (pure rules: pure/impure sequence + set classification, `validate_declaration`, greedy `best_deadwood`; 2-deck duplicate-card handling — two identical pure sequences BOTH valid, sets need distinct suits), `app/games/rummy_engine.py` (deal/draw/discard/declare/drop lifecycle, escrow = 80×point_value per seat, settle with per-player points → refund + rake→revenue-split + XP, `_maybe_autoplay` turn-timer with disconnect≠drop rule + auto-drop after N timeouts, optimistic `rev` concurrency guard, `verify_round`, `quick_match`), `app/routers/rummy.py` (`/api/casino/rummy/tables/{id}/{state,start,draw,discard,declare,drop,heartbeat}`, `/rounds/{id}/verify`, `/quick-match`), catalog entry `rummy_points` (has_turns=True), `rng.shuffled_list`/`verify_list` (generic multi-deck shoe, reused later by Number Game). Frontend `pages/RummyTable.jsx` (dark "modern luxury" theme, live meld-assist checklist + colour-coded group lanes via shared `lib/rummy.js`, turn timer w/ last-5s red pulse, drop-confirm modal, post-round summary w/ per-player deltas, reconnect indicator, provably-fair verify) + `CasinoPage.jsx` game selector + Quick Play. Default rake 70%. Fixes post-test: nested-`<button>` hydration error (RCard `plain` prop) + reload rehydration (tableId+game persisted to localStorage). pytest: `tests/test_rummy.py` (11) + `tests/test_rummy_phase1_flow.py` (5).
- 2026-08: **Casino Commission weekly report — Super Admin (DONE, verified; pytest test_commission_report.py 2/2)**. `GET /api/casino/admin/commission-report?week_offset=N` (SA-only) auto-rolls up the per-round 70% house commission already recorded in `casino_rake_ledger` — Sun–Sat weekly totals (bets = pot, payouts = pot−rake, commission = rake, SA/Admin split, rounds) broken down by game_type (Points Rummy, High Card; **ready for Thane Matka Single/Jodi/Panna/Motor bet-type sub-rows** via the `bet_types` field). Console `CasinoCommissionPanel.jsx` under SUPER_ADMIN → Games nav (stat cards + per-game table + week prev/next). No manual calc; invariant payouts=bets−commission and SA+Admin=commission enforced by test.
- 2026-08: **Casino Commission report — role-scoped + CSV/PDF export + trend chart (DONE, verified)**. Report/trend/CSV/PDF endpoints are now ROLE-SCOPED via `_downline_admin_ids` (SA=platform-wide; ZM=zone's admins; Manager=own admins; Admin=own players — verified SA 172 vs Admin 4). Added `GET /commission-trend?weeks=N` (week-over-week House P&L), `GET /commission-report.csv` and `GET /commission-report.pdf` (reportlab, formatted/printable). Console panel gains CSV+PDF export buttons + a CSS House P&L trend bar chart + role-aware subtitle, and the "Casino Commission" nav item is added to ZM/Manager/Admin sidebars (Reports section). `reportlab` added to requirements.
- 2026-08: **Bonus & Wagering (Playthrough) Foundation — security-critical, shared (DONE, verified; pytest test_bonus.py 6/6 + 26-test regression + live API)**. `app/bonus_service.py`: separate non-withdrawable `bonus_balance` on the wallet + `bonus_grants` ledger with wagering requirement (amount×multiple, default 3×, SA-tunable). `grant_bonus` (idempotent), `record_wager` (advances active grant FIFO, **incrementally releases** bonus→real proportional to playthrough, mode incremental|on_complete), `debit_playable` (spends REAL first then bonus, idempotent — **found+fixed a bug where the idempotency guard ran AFTER the balance check, so replays raised InsufficientFunds**), `expire_bonuses` (scheduler sweep forfeits unreleased bonus), `get_status`, SA-tunable `bonus_config`. New `TxnType.BONUS_GRANT/RELEASE/SPEND/FORFEIT`. Wired into BOTH engines: High Card + Rummy entries now debit via `debit_playable` and call `record_wager` at settle (alongside XP). Router `app/routers/bonus.py` (`/api/bonus/me`, SA `/config`, SA `/grant`). Player UI `components/BonusCard.jsx` on Wallet (bonus balance + play-to-unlock progress bar). Startup index + daily expiry sweep wired in server.py. **This is the shared rail ALL bonus features (VIP recharge, Surprise Box, Festival Gift) must use — none are directly withdrawable.**
- 2026-08: **Weekly Surprise Box (DONE, verified; pytest test_surprise_box.py 4/4 + live UI)**. `app/surprise_box_service.py` — engagement reward for a full Sun–Sat week of play, delivered via the bonus rail (non-withdrawable). END-OF-WEEK ONLY (box is always for the PREVIOUS completed week — can't be banked early); qualify by ≥ `min_rounds` real rounds (from xp_events count) OR ≥ `min_xp`; reward GUARANTEED but HIDDEN until opened; SCALES with weekly play (activity_factor ramp) × weekly recharge × tier cap, floored to a consolation. Lazy idempotent generation + weekly scheduler (`generate_boxes`), 7-day expiry. Endpoints `/api/wallet/surprise-box` (status, amount hidden) + `/surprise-box/open` (reveals + grants). SA config `/api/bonus/surprise-box-config`. UI `components/SurpriseBox.jsx` (animated gift → coin reveal) on Wallet.
- 2026-08: **VIP Recharge Bonus (DONE, verified; pytest test_vip_recharge.py 3/3 + live UI)**. Tier-based STANDING recharge bonus (NOT win-triggered — responsible-gaming-safe). Added `recharge_bonus_pct` per VIP tier (Bronze 0 / Silver 5 / Gold 10 / Platinum 18 / Royal 30) + `recharge_bonus_max_coins` cap to `vip_config` (SA-tunable). Applied at deposit CONFIRM in `deposit_service.confirm_deposit`: base coins → real balance (unchanged), +tier% → bonus rail (non-withdrawable, playthrough), idempotent per `deposit_bonus:{deposit_id}`. Endpoint `/api/wallet/recharge-offer`; AddCoins UI shows the offer + live preview (data-testid `vip-recharge-bonus`).
- 2026-08-14: **Fantasy Demo Fixture + Glass Premium redesign + Promo Codes (DONE, verified iteration_25: backend 13/13 pytest, frontend 100%)**. Unblocked the Dream11-style Fantasy vertical (Sportmonks free tier returns no upcoming fixtures WITH squads) and gave it a flagship UI.
  - **Demo data layer** (`app/demo_data.py`): seeded fixture `demo-t20-royal11` ("Royal Strikers vs Coastal Kings", 22 players d1..d22 with WK/BAT/AR/BOWL roles + mock final batting/bowling stats). `cricket_service.get_upcoming_fixtures()` ALWAYS prepends the demo match (real live fetch capped at 4s + warmed in background so it never blocks the lobby); `get_fixture_lineup()`/`get_fixture_stats()` return demo data for the demo id. `normalize()` gained a `name` field (fixes real fixtures rendering as "TBD vs TBD"). `fantasy_service.ensure_demo_fantasy()` seeds the pool + 3 contests (Free Practice ₹0 / Head to Head ₹100·2 / Mega ₹50·1000) idempotently on startup and repairs stale lock/status on restart.
  - **25% commission**: `COMMISSION_PCT=25` + `default_prize_pool()` (prize pool = 75% of entry×capacity) applied when a contest's prize_pool is unset. `join_contest` now skips the wallet debit for zero-entry (Free Practice) contests. `refresh_player_pool()` (60s TTL) rebuilds the pool from the LIVE squad on each view so player→team affiliation never goes stale across auctions/trades (single source = Sportmonks lineup, same as the Cricket Hub).
  - **Promo Codes** (`app/promo_service.py` + `routers/promo.py`): `POST /api/promo/apply {code}` grants non-withdrawable bonus coins via the shared bonus rail (idempotent per code+user, once-per-player). Seeded demo codes WELCOME100/ROYAL50/FANTASY25. Invalid/reused → 400.
  - **Glass Premium UI** (`pages/FantasyPage.jsx` full rewrite + `pages/fantasy-glass.css` + `lib/teamColors.js` + `lib/avatar.js` + `components/PlayerAvatar.jsx`): frosted-glass cards (~38% black) over a BOLD dominant gradient background. Default theme = ROYAL11/Dream11 red (#EC1C24→#5E080E) with gold (#FFD25A) pills/labels; when a KNOWN IPL team is selected (city-based lookup so "Royal Strikers" correctly stays default, no false RCB match) that team's colours become the dominant gradient + accent. Compact "Your Team Theme" card (short-code badge + A/B team switch). Promo input + gold Apply with micro-interactions (green pulse + sparkle burst + toast + button bounce on success; shake + red flash on invalid). Data-driven "TOP PICK" row per contest (rotated by deterministic form-score from the real pool). Generated DiceBear "shapes" geometric player avatars (seeded by player id, CC0, NO real photos) with initials fallback — used in TOP PICK rows + team-builder. Loading skeleton (no false-empty). Team-builder overlay z-[60] (fixes BottomNav intercepting the Join button on mobile). pytest: `tests/test_fantasy_demo.py` (4/4). PENDING: real Sportmonks live fixtures only surface when the upstream API is responsive (free-tier ReadTimeouts common) — demo always present.

- 2026-08-14: **QUEUED SPEC — Player Support Chatbot (NOT yet built; sequence: after remaining palace popups + 777 Slots)**. AI self-serve help widget for logged-in players. Key requirements: (a) floating widget accessible app-wide; (b) natural-language help that pulls the PLAYER'S OWN read-only context (recent wallet txns, recent game/contest activity, existing tickets) for specific answers; (c) READ-ONLY + canned KB only — bot must NEVER take state-changing actions (no refunds/balance/unlock); (d) "Raise a Support Ticket" escalation that creates a ticket via the EXISTING ticket system pre-filled with the player description + full chat transcript, routed to the existing support role; (e) player sees ticket status / "My Tickets"; (f) support side sees full transcript up front; (g) reuse the EXISTING AI integration (Emergent LLM key / Gemini, as used for Fantasy previews) — no new integration. ADDENDUM: (1) chatbot avatar = original illustrated **Joker** mascot in the splash "Q JOKER" card art style ("wild card fits everywhere"); (2) human support login — REUSE the existing `SUPPORT_HELPER` role/credentials (already built: support_service.py + SupportPanel.jsx, scoped to tickets only, no financial perms) rather than creating a new role; (3) the SUPPORT_HELPER dashboard must be a genuinely usable end-to-end ticket queue (open tickets → open one → see chat transcript + player context → respond → mark in-progress/resolved), not read-only. Check existing `support_tickets` collection + `SUPPORT_HELPER` role before building.

- 2026-08-14: **Royal Palace visual polish — Cards tables (DONE, verified iteration_29 100%)**. Extended the Vegas felt work into a "royal casino palace" look: generated original illustrated **palace backdrop** + **royal host portrait** (`lib/casinoAssets.js`; no real people/brand assets) applied behind both card tables via `.vegas-palace` + gold-framed `.vegas-host`; richer **seat chips** (DiceBear avatar in a gold conic ring + name tag + balance/cards pill + active-turn glow); glossier (`rich`) **suit-sorted hand**. Fixed a CRITICAL card-sizing regression (`.pc` span needed `display:inline-block` — face-up cards were collapsing to ~2x24px). Also fixed (verified it_28) the **stale-localStorage infinite-spinner** bug on /casino: a persisted invalid table id looped on HTTP 400 forever → now clears the `royal11_casino_table`/`royal11_casino_game` keys and falls back to the lobby on 400/404 (both RummyTable & CasinoPage). REMAINING palace scope (BACKLOG, not yet built): lobby/home reskin, ornate Win/Bonus celebration popup, in-table pause/menu overlay, round-end Scoreboard popup, low-balance "Not Enough Chips" popup, ornate Store/coin-purchase popup.

- 2026-08-15: **Royal Palace overhaul completion (DONE, verified iteration_30 100%)**. Finished the remaining palace scope. Ornate popups wired into Rummy (`components/casino/OrnatePopups.jsx`: `WinCelebration`, `Scoreboard`, `LowChipsPopup` — the low-balance nudge now surfaces the ornate "Not Enough Chips" popup, whose "Get Chips" opens the store). Cards **lobby reskin** (`CasinoPage.jsx`): dark palace backdrop + gold/red ornate game-selector tiles + table cards (`.palace-lobby`/`.palace-tile` in `casino-vegas.css`). **Store reskin**: `AddCoins.jsx` gained a `palace` prop (ornate dark chrome + "Royal Coin Store" title) used when opened from the Rummy table; the Wallet usage stays light. Fixed a pre-existing broken CSS import path in `OrnatePopups.jsx` (`../pages` → `../../pages`).
- 2026-08-15: **777 Slots (DONE, verified iteration_30: backend 12/12 pytest + frontend 100%)**. New provably-fair, single-player, house-banked 3-reel / single-payline slot on the SAME commit-reveal RNG. `app/games/slots.py` (pure math: 6 ROYAL11 symbols Crown/Seven/Diamond/Bell/Star/Coin, per-reel weights 1/2/3/4/6/16=32-stop strip, paytable 450×/66×/22×/9×/5×/1.6× → **RTP 0.2985 ≈ 70% house**, `rtp()`/`evaluate()`), `app/games/slots_service.py` (**seed-pair provably-fair**: per-player `player_game_seeds` = secret server_seed + published SHA256 commit + player client_seed + incrementing nonce; spin derives stops via `rng.spin_indices` HMAC stream; idempotent stake debit via bonus rail, payout credit, house margin → `casino_rake_ledger` game_type=slots_777, XP + bonus playthrough; rotate reveals old seed into `revealed_game_seeds` so past spins recompute; `verify` recomputes after rotate). `rng.seed_commit`/`rng.spin_indices` added. Endpoints under `/api/casino/slots/*` (config, seed, seed/client, seed/rotate, spin, history, verify/{id}) + SA `/api/casino/admin/slots-config`. Frontend `pages/SlotsPage.jsx` + `slots.css` (Vegas cabinet, marquee bulbs, staggered reel stops, near-miss tease on 2-of-a-kind big symbols, coin-burst win overlay + jackpot shake, paytable, provably-fair panel w/ client-seed edit + rotate + verify, recent-spins) + `lib/slotAssets.js` (6 generated original symbol images) + `lib/slotSound.js` (Web-Audio SFX, toggleable, OFF by default). Added as 3rd game tile in `CasinoPage.jsx`. pytest `tests/test_slots.py` + `tests/test_iter30_slots_chatbot_mint.py`.
- 2026-08-15: **Super Admin "Generate Coins" (mint) console panel (DONE, verified iteration_30)**. AUDIT RESULT: coin generation was always present but labeled "Fund" (per-row action in Managers/Zonal Managers) — `wallet_service.credit()` increments a recipient wallet with NO source debit = true minting (PRD line: "SA mints into ZM wallet"). Platform is NOT recharge-only; root supply = SA mint → ZM → Manager → Admin → Player. Added a discoverable, clearly-labeled surface: `GET /api/admin/coin-supply` (SA-only: total_minted from SUPER_ADMIN_TO_MANAGER+SUPER_ADMIN_TO_ZONAL ledger, coins_in_circulation, recipient list w/ balances) + console `CoinSupplyPanel.jsx` ("Generate Coins" nav, top of Coins & Financials) — stat cards + a mint form (pick ZM/Manager → amount → Generate) reusing the existing `/fund` endpoints. Hierarchy completeness pass: all roles' core actions confirmed wired (SA/ZM/Manager/Admin/Support Helper).
- 2026-08-15: **Player Support Chatbot "Q JOKER" (DONE, verified iteration_30)**. Floating, player-only AI help widget reusing the EXISTING Emergent LLM (Gemini `gemini-3-flash-preview`) — no new integration. `app/chatbot_service.py` builds a READ-ONLY context snapshot (real+bonus balance, recent txns/deposits/slot spins, open tickets) and a strict system prompt (Q JOKER persona; NEVER performs/promises account actions — refuses refunds/credits/unlocks; offers a ticket instead). Sessions stored in `chatbot_sessions`. `app/routers/chatbot.py`: `POST /api/chatbot/message`, `GET /api/chatbot/session/{id}`, `POST /api/chatbot/escalate` (creates a REAL ticket via existing `support_service.create_ticket`, embedding the full transcript, auto-routed to the player's assigned Admin/SUPPORT_HELPER). Frontend `components/SupportChatbot.jsx` (mounted in `App.js` ProtectedShell, PLAYER-only): illustrated Q JOKER mascot launcher, chat panel w/ quick-replies, "Talk to a human · Raise a ticket" escalation form. Verified: relevant replies, refuses state-changes, escalation ticket visible to admin1 + a runtime SUPPORT_HELPER with full transcript.
- 2026-08-15: **Codebase audit (report only)** — produced `/app/AUDIT_REPORT_2026-08-15.md` (tech stack, structure, user/rummy/realtime/coin/admin/security/DB/API/tests, production-readiness). Key confirmations: 5-level hierarchy (SA→ZM→Manager→Admin→Player) with atomic quota reservation; Points Rummy only (no Pool/Deals); polling (no WebSockets); no withdrawal (deposit inflow exists). Flagged: `wallet_service.transfer` non-atomic (no Mongo txn), spins not idempotent to double-submit, login-only rate limiting.
- 2026-08-15: **Admin→Super Admin USDT (TRC-20) coin-purchase channel (DONE, verified iter31 100% + iter32 100%)**. Internal B2B top-up, separate from player INR deposits, mirroring the manual proof-submit + verify trust model. ONE static Super-Admin receiving wallet (typed address + uploaded QR image), Super-Admin-configurable `coin_rate` (default 1.5 coins/₹) and `min_inr` (default ₹100000). Backend: `app/crypto_purchase_service.py` + `app/routers/crypto_purchase.py`; new collections `crypto_purchase_requests` + `crypto_config`; new `TxnType.CRYPTO_PURCHASE`. Flow: Admin submits usdt_amount + inr_equivalent + **own sending wallet** + proof (tx id/screenshot) → PENDING; Super Admin reviews queue → Confirm auto-credits `inr*coin_rate` via idempotent `wallet_service.credit` (`request_id=crypto_purchase:{id}`) → shows in Admin txn history; Reject with reason (no credit). Endpoints: `/api/admin/crypto/config|purchase-request|my-purchases|qr`; `/api/superadmin/crypto/config[/qr]|requests|requests.csv|admins|requests/{id}/screenshot|confirm|reject`. Every record permanently stores admin id/name, sender wallet, receiving wallet at time of request, usdt amount, inr-equiv, coin_rate, coins credited, proof, status, submitted/decided timestamps + `decided_by(_name)`. `/api/admin/coin-supply` now returns distinct `total_crypto_purchased` (separate from `total_minted` + `total_inr_deposits`). Super-Admin report screen with status/admin/date filters + CSV export; all actions written to `audit_logs` (crypto_purchase.request/confirm/reject). Frontend: `console/CryptoBuyPanel.jsx` (ADMIN nav "Buy Coins (USDT)"), `console/CryptoAdminPanel.jsx` (SA nav "USDT Purchases": settings + QR upload + history/report + CSV), `console/AuthImage.jsx` (authed blob image). pytest `tests/test_crypto_purchase_iter31.py` 13/13.
- 2026-08-15: **Weekly INR Settlement (Admin↔Super Admin) + bank-format-aware bulk payout export (DONE, verified iter33: 21/22 pytest + frontend 100%)**. Extended the EXISTING `revenue_service` settlement engine (Sun–Sat weeks, deposit-based split × configurable `revenue_split_super_admin_pct` — user chose 1a) rather than rebuilding. **Overdue handling changed** from instant hard-suspend to **3-day grace** (`SETTLEMENT_GRACE_DAYS`): overdue-but-in-grace → warning banner + one-time notification, no suspend; past grace → auto-suspend (SuspendReason.SETTLEMENT_OVERDUE). A suspended Admin can STILL hit `/admin/settlement/{id}/pay` (no `require_not_suspended`), and submitting proof lifts the overdue suspension (payment-in-transit safety). **Settle flow (3b):** Admin sees "My Settlements" → "Settle Now" (company bank instructions + reference/screenshot proof) → status SUBMITTED; Super Admin confirms in the Settlements queue (sees reference + proof image) → SETTLED; Super Admin may also mark PAID directly without proof. **Company remittance account (4a):** single Super-Admin-managed account in `settlement_config._id=company_bank`, shown to Admins. **Bank Payout Templates:** `app/bank_template_service.py` + collection `bank_payout_templates`; 6 seeded starters (Universal, HDFC, ICICI, SBI, Axis, Union) with common columns (Beneficiary Name/Account Number/IFSC/Amount/Mode/Remarks) flagged "verify before real use"; fully editable (add/remove/reorder columns, header text, field mapping); per-Admin assignment stored on `admin_allocations.bank_template_id`. **Bulk CSV export (5a):** `/superadmin/payouts/export` renders the chosen template's column order with each beneficiary Admin's resolved bank details (from their active `admin_bank_accounts`); **blocks export (400) if any selected beneficiary has no bank on file**. Endpoints in `app/routers/settlement.py`: `/admin/settlement/my|company-bank|{id}/pay`; `/superadmin/settlement/company-bank|proof/{id}`; `/superadmin/payouts/fields|templates[CRUD]|assign|admins|export`. All template/payout/settlement actions logged to `audit_logs`. Frontend: `console/AdminSettlementsPanel.jsx` (ADMIN "My Settlements"), extended `console/SettlementsPanel.jsx` (company bank card + SUBMITTED confirm + proof view), `console/BankPayoutsPanel.jsx` (SA "Bank Payouts": template library editor + assignment + bulk export). pytest `tests/test_settlement_payouts_iter33.py` 21/22.


- 2026-08-14: **Independence Day Festival Bonus (DONE, verified iteration_26 100% + pytest test_festival.py 2/2)**. `app/festival_service.py` — one-tap claimable **151** non-withdrawable coins via the shared bonus rail, once-per-player (idempotent `festival:independence_2026:{uid}`), server-enforced time window (Aug 13–16). Endpoints `GET /api/bonus/festival` + `POST /api/bonus/festival/claim` (audit-logged FESTIVAL_BONUS_CLAIMED; every credit hits the bonus ledger for SA visibility). Frontend: `components/IndependenceBanner.jsx` gains a gold "Claim 151 Coins" button (Music toggle kept) → celebratory toast + sparkle + disabled "Gift Claimed" state that persists.
- 2026-08-14: **Referral Program enhancements + In-app Notifications (DONE, verified iteration_26 100% + pytest test_referral_notify.py)**. Tuned modest defaults (referrer 125 / referee 75, both <= festival 151). Referee bonus on signup-with-code; referrer bonus ONLY after the referee's FIRST_RECHARGE (fraud-safe), which now **creates an explicit named notification** to the referrer. New `app/notification_service.py` + `routers/notifications.py` (`GET /api/notifications`, `POST /api/notifications/read`, idempotent create). New `components/NotificationBell.jsx` wired into HomePage (live badge + panel + one-time success toast per new unread; polls 30s). `referral_service.admin_stats()` + SA-only `GET /api/referrals/admin/stats` (totals + bonus paid to referrers/referees). `ReferAndEarn` gains Independence "Freedom to Play" copy, clearer per-friend status labels (Joined pending recharge / Recharged Bonus earned +X), a milestone nudge, and empty-state. Self-referral blocked; cap via `max_referrals_per_user` (SA-tunable). NOTE: legacy referral rows show their historically-paid reward (e.g. +200) by design.
- 2026-08-14: **Vegas Cards redesign + Cash-table "Couldn't join" fix (DONE, verified iteration_27: backend 4/4 pytest, frontend 100%)**. New `components/PlayingCard.jsx` (+ `pages/casino-vegas.css`): realistic glossy card faces (corner pips + center suit / gold face-card badge), branded ROYAL11 red/gold "R11" card back for face-down, joker art — pure CSS, no licensed assets. Both High Card (`CasinoPage.jsx`) and Rummy (`RummyTable.jsx`) tables now render on a deep-red **Vegas felt** surface (gold rail, vignette, spotlight) with **DiceBear avatar seats** (card count, score, active-turn glow) — High Card seats laid out around an oval, Rummy as seat chips; branded card-back deck pile. All functional UI preserved (grouping chips, deadwood, Discard/Declare/Drop, draw piles, Provably Fair). **BUG FIX**: the lobby showed an active Join on RUNNING/full tables -> failed with "Couldn't join"; now only WAITING & not-full tables show Join, others show a disabled "In progress"/"Full" label (data-testid `casino-table-status-<id>`), plus refresh-on-failed-join + 10s lobby poll to kill the race. Hardened `engine._round_view` to no-op on rummy round shape (was a 500 via generic `/state` on a RUNNING rummy table). pytest `tests/test_casino_join_fix_iter27.py` (4/4).


## Backlog / Remaining
- **NEXT MILESTONE — Thane Matka build spec (fair, provably-fair; NOT yet built).** Concrete plan for a clean, fully-fair MVP:
  - **Result model:** each draw produces a 3-digit **Panna** (d1,d2,d3 each 0–9 from the committed RNG); **Ank/Single** = (d1+d2+d3) % 10. RNG: add `rng.draw_digits/commit_digits/verify_digits` (reuse `_SeedStream`). Commit `SHA256(server_seed|digits)` at draw creation (BEFORE open), reveal `server_seed` at `result_at`.
  - **Bet types + FAIR fixed odds @ 30% RTP (70% house):** Single/Ank match (1/10 → **3×**); Jodi = last two digits (1/100 → **30×**); Panna = exact 3-digit (1/1000 → **300×**); Motor = pick a set S of single digits, stake split evenly across |S| singles, each matched pays 3× on its portion (keeps 30% RTP). Multipliers = total return incl. stake; all SA-configurable in `matka_config`.
  - **Draw lifecycle (no separate scheduler needed):** SA-configured Open/Close slots create `matka_draws` {open_at,close_at,result_at,commit_hash,server_seed(secret),digits(hidden),status}. Lazy `_advance(draw)` on read/bet transitions scheduled→open→closed→revealed (settles at result_at) — same lazy pattern as surprise_box/rummy autoplay; bets only accepted in [open_at,close_at].
  - **Settlement + commission:** per draw+bet_type+admin_id, record into `casino_rake_ledger` with game_type="thane_matka", bet_type∈{single,jodi,panna,motor}, pot=Σstakes, rake=pot−Σpayouts → auto-appears in the Casino Commission report's bet-type sub-rows (already built for it).
  - **Provably fair:** `/verify` recomputes digits from revealed seed+nonce vs commit. **Name** default "Thane Matka" (SA-configurable). **Card-flip reveal** (pure UI): map each result digit to a standard 52-card face A=1…K=13 (NO Joker), animated flip w/ suspense (flagship polish bar). Player lobby: pick market/bet type, place bet in window, watch card-flip reveal at result time.
  - pytest: fair-odds RTP check (Σ prob×payout == 0.30 per type), lifecycle transitions, settlement math, verify.
- 2026-08: **SA "Rewards & Bonuses" config panel (DONE, verified — save+persist)**. Console panel (SUPER_ADMIN → Games) to tune, from the UI: the Bonus Rail (`/bonus/config`), VIP Tiers (`/casino/admin/vip-config` — per-tier min_xp / recharge_bonus_pct / rakeback_pct + recharge_bonus_max_coins), and Weekly Surprise Box (`/bonus/surprise-box-config`). `console/RewardsConfigPanel.jsx`. Fixed a missing `put` method in `console/api.js` (was blocking all Console PUT saves).
- ✅ **Real Coin Actions** (Rewards Store, Lucky Spin, Boosts wired to real ledger) — DONE (2026-08, iteration_16).
- ✅ **QR Amount Prefill** for deposits — DONE (2026-08, iteration_16).
- ✅ **Payroll Payslips + Missed-Week Catch-up** — DONE (2026-08, iteration_16).
- ✅ **Fantasy Cricket Phase 1** (contest system, team builder, auto-settlement) — DONE (2026-08, iteration_17). **USER VERIFICATION PENDING.**
  - ✅ Task 3 — Salary + Incentive payroll (Managers & Zonal Managers) — DONE (2026-08, iteration_15).
  - ✅ Zonal Manager tier + Admin-creation approval + per-Manager cap — DONE (2026-08, iteration_13).
  - ✅ Multi-bank accounts + UPI QR + nickname; WhatsApp contact — DONE (2026-08, iteration_14 & 15).
- **QUEUED (2026-08, user-requested, in order) — security & console UX:**
  1. ✅ **P0 Brute-force login protection — DONE (verified iteration_19, 100%; pytest test_login_security.py 4/4)**. `app/login_security.py` (MongoDB per-email + per-IP counters, X-Forwarded-For IP, lockout+cooldown, clear-on-success), `auth.py` 429 gate, `app/routers/security.py` SA alerts, Console `SecurityPanel` ("Login Security", SA only). Per-email 5/15min→30min; per-IP 20/15min→30min; env-tunable.
  2. ✅ **P0 Email OTP at player registration — DONE (verified iteration_19, 100%)**. `app/otp_service.py` (sha256 6-digit, 10-min TTL, ≤5 attempts, 30s resend cooldown) + `app/email_service.py` (Emergent-managed Resend). register→PENDING_VERIFICATION+email; verify-otp activates (welcome bonus+assign+token); login 403 until verified. Frontend OTP step in `AuthPage` + `AuthContext`. `OTP_DEBUG_LOG=true` logs codes for tests.
  3. ✅ **P1 Console Overview layout refinement — DONE (verified iteration_20, 100%)**. `console/OverviewPanel.jsx` rewritten (bank-style, ROYAL11 identity): This Week/All Time segmented pill toggle, masked balance hero with eye-reveal (count-up on reveal), Quick Action icon tiles (Deposits/Settle Payouts/View Report CSV/Transactions), collapsible Recent Activity, right-column "Manage" Quick Links grid (navigate to panels). `ConsolePage.jsx`: circular sidebar icon badges + highlighted active, top-bar Help pill + user + theme switcher. All figures remain real (server-side).
  4. ✅ **P1 Console theme switcher (per-user) — DONE (verified iteration_20, 100%)**. `console/ThemeSwitcher.jsx` (top-bar Palette menu, 4 themes: default "Cherry & Sky", Dark, Sky Blue & White, Navy & Gold) + `console/theme.css` (scoped skins on `.console-root[data-theme]`, default = no overrides). Persisted PER ACCOUNT via `PUT /api/auth/console-theme` + `UserPublic.console_theme` (survives relogin/reload/devices). Skin-only; layout identical across all Console roles.
- **QUEUED (2026-08) — PLAYER APP (separate from Console queue), slot after Console items:**
  - ✅ **P2 Favorite-team splash audio — DONE (verified iteration_20, 100%)**. `components/SplashScreen.jsx` plays one shared CC0 (no-attribution) crowd-hype clip `frontend/public/royal11-hype.mp3` (~4.2s, sourced from Wikimedia Commons, trimmed via ffmpeg) during the splash ONLY when `localStorage['royal11_fav_team']` is set. Autoplay-safe (silent fail if blocked). Same clip for every team (no franchise anthems). Closes the old "Cricket crowd-chant sound design" idea.
- ✅ **Player login page enhancement — DONE (2026-08)**. `/auth` + branded `/login/<slug>` restructured into a two-column marketing layout (bank-homepage style): left `components/LoginShowcase.jsx` (CSS-only 3D spinning coin `pages/login-hero.css`, heading, 4 trust tiles — Server-Verified Results / Secure UPI Payments / 24/7 WhatsApp Support / Instant Fantasy Contests, "How it works" 3-step strip); right = the UNCHANGED login/signup/OTP form. Form-first on mobile (stacks, scrolls), side-by-side on desktop (form above fold). Branded variant still shows the Admin's brand_name/logo. No extra asset weight (pure CSS 3D).
- **P1 (NEXT): Fantasy Football Phase 2** — extend the Dream11-style system to Football, mirroring the Cricket pattern (Sportmonks football data → player pool → 11-man builder w/ 100 credits, C/VC → server-side settlement/payouts). NOTE: Sportmonks Football is a separate product/subscription; may need a new API key.
- **P2: Multiple-teams-per-contest** — let a player submit multiple different teams (Team A, Team B) into the same contest (Phase 1 assumes 1 team per user per contest; `fantasy_teams` has a unique `(contest_id, user_id)` index that must be relaxed).
- **P1: My Contests live rank** — show live rank/points in the "My Contests" tab during a match (currently only final rank/winnings after SETTLED).
- P1: Admin Console — Phase 2 (Figma frames not yet built): Users/Players management, Rewards & Game admin, Reports, Notifications, Support, Settings. NOTE: Deposit Mgmt / Withdrawal Mgmt / Bank Accounts nav from Figma are on HOLD (non-withdrawable model) — do NOT build.
- P1: Manager "Assign Players" UI (backend `POST /admin/players/assign` + `GET /admin/players` already exist).
- P2 (refactor): `admin.py` is ~680 lines — split into feature routers (managers/admins/deposits/settlements/recharge); consider a shared DomainError for uniform 400/404 mapping.
- P1: Real ledger endpoints for game mechanics so the "Coming soon" actions go live:
  Earn Coins (daily/achievement credit), Rewards Store purchases (debit + owned items),
  Lucky Spin (entry debit + reward credit), Boosts, Fantasy Contest join/lock, Streak claim.
- P1: My Contests screen (joined contests with live rank); Match Alerts (wicket/goal toasts).
- P1: Streak Calendar (7-day + day-7 milestone); Spin Cooldown (free daily spin).
- P2: Boost timer on more surfaces; extend AI Coach with per-match context; refresh token flow.

## Rummy + Casino card games (real-money, coin wallet) — approved 2026-08
**Hard principle (locked):** server is the SOLE, genuinely-random source of truth for every outcome. Provably-fair **server-seed commit–reveal**: full deck/wheel is derived from a CSPRNG `server_seed`+`nonce`, its SHA-256 committed BEFORE any action, revealed after + independently verifiable via `/verify`. No admin control, no stake-size weighting. **Humans only, no bots.** Rake flows into the existing Admin/Super-Admin revenue split.

- ✅ **Phase 0 — Foundation — DONE (2026-08; pytest test_casino.py 5/5 + curl e2e verified)**. `app/games/rng.py` (commit–reveal, HMAC-Fisher–Yates unbiased shuffle, verify), `app/games/cards.py`, `app/games/catalog.py` (game registry), `app/games/engine.py` (tables/rounds/seats, idempotent coin entry/payout via `wallet_service`, rake→`casino_rake_ledger` with Admin/SA split, provably-fair state + verify), `app/routers/casino.py` (`/api/casino/catalog|tables|tables/{id}/{state,join,leave,start}|rounds/{id}/verify|admin/rake`). Demo game **High Card** (instant showdown) exercises the whole flow. Frontend: `pages/CasinoPage.jsx` lobby+table shell with provably-fair Verify panel; "Cards" bottom-nav tab. Transport = REST + ~1.5s polling (WebSocket = later phase).
- **Phase 1 — Rummy — ✅ DONE (2026-08, iteration_21).** Points Rummy — Points variant built (Pool 101/201 + Deals + tournaments = Phase 1.5, not yet built). Turn/action engine (draw/discard, set/sequence/pure-sequence validation, declare/drop, scoring). Defaults: point value 1/2/5 coins, **default rake 70%**, 2–6 seats. Live meld-assist, turn-timer, drop-confirm, post-round summary, quick-match, disconnect≠drop all shipped.
  - **Duplicate-card handling (2-deck shoe for 5–6 seats):** every dealt card is a DISTINCT physical instance (stable `instance_id`), validated by structure on rank/suit but assigned to a group by instance — so TWO separate Pure Sequences using identical values (e.g., 5♠6♠7♠ twice) are BOTH valid. Sets still require distinct suits (two 7♠ in one set = invalid); jokers substitute. Add explicit pytests for both cases.
  - **Live meld-assistance UI (real-time):** as the player drags cards into group lanes, each group shows a live label (Pure Sequence ✓ / Sequence ✓ / Set ✓ / Incomplete / Invalid, colour-coded) plus a running header checklist (Pure Sequence ✓ · 2nd Sequence ✓ · All grouped ✓ · Provisional score). Recalc on every move via a SHARED validator (client-side for instant feedback; server re-validates authoritatively at Declare — Declare only enabled when client says valid). Reuse the Fantasy `TeamBuilder.jsx` drag/group pattern. No "Check" button.
  - **UX additions (player-experience only, no money/rake impact):**
    1. **Turn-timer warning** — client computes remaining time from the engine's `turn_deadline`; last 5s → amber→red pulse + optional muted-by-default tick. Server still auto-plays at the deadline (warning is just a heads-up).
    2. **Disconnect/reconnect** — inherent to REST+Mongo+polling: `GET /state` rehydrates the exact round on reconnect. Rule: **disconnect ≠ drop** — auto-play/drop fires ONLY on turn-timer expiry, never on a missed poll. Add `last_seen` heartbeat + "reconnecting…" UI + configurable grace (skip only after N consecutive turn timeouts).
    3. **Drop confirmation** — client confirm dialog ("Drop this hand? Concede N points") before the Drop POST (`data-testid=drop-confirm`).
    4. **Post-round summary** — server extends settle result with a per-player breakdown (each group valid/invalid, pure-sequence present?, why a declaration failed, points); rendered in a post-round modal so players learn from outcomes.
    5. **Quick match** — `POST /api/casino/quick-match {game_type, stake, is_practice}` joins the first WAITING table with a free seat matching stake+mode, else creates+joins one; frontend "Quick Play" button.
- **Phase 2: Teen Patti + Andar Bahar** (betting-round engine, pot rake).
- **Phase 3: Roulette + Blackjack** (house-banked; wheel RNG, dealer logic, bankroll guard).
- **Number Game (SA-named numbers game, formerly "Matka" internally) — PROPOSED 2026-08, NOT BUILT (architecture-first, awaiting decisions).** House-banked fixed-odds numbers game on the SAME provably-fair RNG (commit–reveal + `/verify`), zero manipulation — the earlier admin-controlled/rigged version was rejected and never built; this is the legitimate house-edge model like Roulette. Reuses `casino_tables`/`casino_rounds`/wallet/rake-split/REST-poll; NEW = `rng.draw_digits`/`draw_weighted` (generalise commit-reveal to digits — the `rng.shuffled_list`/`verify_list` added for Rummy is the same groundwork), a house-banked betting-window round type, per-bet records, bankroll/max-payout guard. Payout multipliers derived from TRUE probability × RTP: default **70% house edge → 30% RTP** (Super-Admin-configurable) ⇒ Single-digit (1/10) pays **3×**, Jodi/pair (1/100) pays **30×** (total returned incl. stake). Likely built FIRST within Phase 3 as it pioneers the banked betting engine Roulette/Blackjack reuse.
  - **Display name is Super-Admin-configurable** (game catalog / branding settings, same SA-config pattern as vip-config/scoring-config). Stored in an SA-editable config doc (`game_display_names` or per-game `display_name`), surfaced everywhere the game is shown. **Current default set to "Thane Matka"** per explicit user business decision (user informed this is a real illegal-betting-circuit name that raises enforcement risk vs a generic name; proceeding anyway, handling compliance themselves). SA can rename anytime.
  - **Scheduled timed draws (Open/Close, multi-slot per day, SA-configurable) — fairness model UNCHANGED.** New `matka_draws` lifecycle wraps the EXISTING commit-reveal RNG (no change to `rng.py`): scheduler pre-creates each draw slot and generates `server_seed+nonce` + publishes `commit_hash` **BEFORE the betting window opens** (result locked + committed before any bet). Bets accepted only during `[open_at, close_at]`. At the scheduled `result_at` the scheduler REVEALS `server_seed` → players `/verify` against the pre-published commit → payouts settle. The schedule ONLY controls WHEN the already-committed fair result is revealed; there is NO path to choose/influence the number after seeing betting. New pieces = `matka_draws` collection (`draw_id, market, open_at, close_at, result_at, commit_hash, server_seed[secret until reveal], result, status: scheduled→open→closed→revealed`), APScheduler pre-commit + reveal/settle jobs, per-bet records, bankroll guard. Multiple Open/Close slots/day = multiple draw rows. NUANCE (inherent to all commit-reveal, unchanged): server holds plaintext seed in DB between commit and reveal (guarantee = cannot CHANGE result post-commit, not operator-blind); optional future-hardening = mix a public future entropy source (e.g. blockchain block hash at result_at) for operator-blind draws.
  - **Card-flip result reveal (PURELY presentation — no RNG change).** Present the already-decided, provably-fair result number via an animated **playing-card flip** reveal (evoking traditional Matka's cards-drawn presentation): cards flip/turn to reveal each digit with suspense/anticipation staggering (ties into the same flagship polish bar as Slots — satisfying reveal motion, clean digit typography on the card face, optional toggleable sound). **Card value convention = standard playing-card numbering: Ace=1, 2..10 = face, J=11, Q=12, K=13.** Uses ONLY the standard 52-card deck (A=1..K=13 per suit) — the **Joker is EXCLUDED** from the reveal card set (it has no numeric value here). The card art is only a stylish display of the number already produced by the commit-reveal RNG (locked before the betting window closes); it is NOT a mechanism for generating/deriving the result. Fits the existing flow: at `result_at` the revealed digits are simply mapped onto card faces client-side and animated in.
- ⚠️ **COMPLIANCE — "Thane Matka" (record, re-surface pre-launch):** Naming the game "Thane Matka" AND using real-matka-style scheduled multi-draw Open/Close timing SIGNIFICANTLY increases resemblance to an actual named illegal betting operation — legal/regulatory/enforcement risk BEYOND the generic "numbers game" version. Still the HIGHEST legal risk of all requested games (no game-of-skill defence). Not legal advice; user explicitly informed of the added risk from the real name + timed-draw structure and is proceeding as a business decision, handling compliance themselves. MUST re-surface before any production launch.
- **Slots (3-reel slot machine) — PROPOSED 2026-07, NOT BUILT (architecture-first, awaiting approval).** House-banked, single-player-vs-house pure-chance game on the SAME provably-fair RNG. **Non-negotiable:** each spin's reel outcome is generated + committed server-side BEFORE the visual spin animation plays; the animation is only a REVEAL of an already-locked-in result. Uses the industry-standard **seed-pair + nonce** provably-fair model (stronger than per-round fresh seed, correct for instant single-player games): server pre-commits `SHA256(server_seed)` to the player once, player supplies a `client_seed`, each spin derives its outcome from `HMAC(server_seed, client_seed:nonce)` with an incrementing nonce; on seed rotation the old `server_seed` is revealed so the player can recompute every past spin. New generalised `rng.draw_weighted(seed_material, weights)` (HMAC stream + rejection sampling, reuses `_SeedStream`), shared with Matka/Roulette.
  - **Symbols (original ROYAL11 art, gold/premium):** Royal Crown (jackpot), R11 Seven, Diamond, Royal Bell, Star, ROYAL11 Coin (low). NO copied mascots/branding (explicitly NOT the "fox mascot" / "Crazy Chicken Jackpot" reference designs).
  - **Math (RTP consistent with 70% house edge / 30% RTP default, Super-Admin-tunable + pytest-validated):** 3 independent reels, 32-stop virtual strip each. Per-reel weights Crown 1 / Seven 2 / Diamond 3 / Bell 4 / Star 6 / Coin 16 (=32). Pay on 3-of-a-kind on the single centre payline (MVP). Example paytable (× stake, total return): Crown 450× / Seven 66× / Diamond 22× / Bell 9× / Star 5× / Coin 1.6× → RTP ≈ 29.85% (final multipliers locked to sum to exactly the configured RTP, verified by a pytest that computes Σ p³·M).
  - **Data model:** REUSE `wallet_service` (idempotent debit/credit), `revenue_service` split, `progression_service` XP, `practice_service` chips, and the `/verify` verification pattern. NEW = `casino_spins` collection (per-spin banked record: user, stake, reel stops, symbols, payout, seed refs, nonce) + `player_game_seeds` (server_seed/hash/client_seed/nonce/revealed). House margin (stake − payout) aggregated per settlement window and fed into the Admin/Super-Admin revenue split (banked games net positive over time; per-window aggregation avoids negative per-spin attribution). Bankroll guard: `max_stake` + `max_payout_cap` config.
  - **Endpoints (proposed):** `GET /api/casino/slots/config` (paytable+symbols+RTP), `GET /api/casino/slots/seed` (current commit hash + set client_seed + rotate/reveal), `POST /api/casino/slots/spin {stake,is_practice}`, `GET /api/casino/slots/history`, `POST /api/casino/slots/verify {spin_id}`, SA `GET/PUT /api/casino/admin/slots-config`.
  - **Visual (Slots-only, distinct from restrained Rummy):** classic Las Vegas "movie Vegas" aesthetic — neon signage, glowing marquee-bulb border around the cabinet, classic slot-cabinet frame + pull lever, bold red/gold/black, flashing lights around the reels, poker-chip + dice background motifs, big glowing/shiny jackpot text. Original ROYAL11 art only. Casino section may lean vibrant/glitzy vs Rummy's deep-charcoal + slim-gold-hairline "modern luxury".
  - **Sequencing:** within Phase 3 (house-banked line-up); can pioneer the banked engine that Matka/Roulette/Blackjack reuse.
  - **⭐ FLAGSHIP POLISH (user priority — build with extra care; this is meant to be the most "fun/premium" game + a casino draw):**
    - **Spin feel:** satisfying reel spin-up (accelerate), **staggered reel stops** (reel 1 → 2 → 3 land sequentially), and **near-miss anticipation** — when 2 reels match a big symbol, the 3rd reel slows/teases before stopping. Motion via the `motion` (framer) library + CSS; easing tuned for weight/momentum.
    - **Win reveal:** exciting — flashing marquee lights, **coin/chip burst particle effect**, glowing animated payout count-up text, winning-line highlight. **Distinct, bigger celebration for the Crown jackpot** (full-screen fanfare, screen-shake/confetti, "JACKPOT!" treatment) vs ordinary wins.
    - **Sound design (toggleable, off-by-default, CC0/royalty-free, autoplay-safe):** reel-spin loop, reel-stop clicks, win chime (scaled to win size), jackpot fanfare. PLUS a low-volume **ambient casino-floor bed** — distant crowd murmur/chatter, faint other slot machines running/dinging, and coin-clinking — layered UNDER the main game sounds (never overpowering them) to evoke a busy real casino floor. The ambient bed follows the same rules: toggleable, off-by-default, CC0/royalty-free, autoplay-safe; loops seamlessly and ducks/sits below reel+win SFX (separate lower gain). Global mute toggle persisted in `localStorage['royal11_sound']` (same pattern as splash audio); silent-fail if blocked.
    - Must still obey the non-negotiable provably-fair rule: the outcome is committed/decided BEFORE the animation — all the above is pure presentation of an already-locked result.
- ⚠️ **COMPLIANCE — Slots (record, re-surface pre-launch):** Slots are pure-chance with ZERO skill component — the highest-risk category in the casino line-up (the most universally recognised "gambling machine", heavily restricted almost everywhere including India, no game-of-skill defence). Not legal advice; user is aware and has chosen to proceed with the same non-manipulable fair-RNG requirement, handling legal/regulatory compliance themselves. MUST re-surface before any production launch.

### Roadmap additions — PROPOSED 2026-08 (architecture-first, not built)
- **Player-app desktop responsiveness — Phase R1 ✅ DONE (2026-08, frontend testing agent iteration_22 100%).** Global responsive shell: mobile keeps `BottomNav` (`md:hidden`); at `md` (≥768px, iPad portrait / Z Fold unfolded) a collapsed **76px icon-rail** left sidebar; at `lg` (≥1024px) a full **256px labelled sidebar** (`components/SideNav.jsx`, `hidden md:flex w-[76px] lg:w-64`). `App.js ProtectedShell` wraps routes in `<div className="md:pl-[76px] lg:pl-64">`. Nav items shared via `lib/navItems.js` (SideNav + BottomNav in sync). Min 48px tap targets on rail items. Console unchanged (already desktop). Phase R2 (per-page multi-column reflow) still pending.
- **Rummy Table Themes + Ambiance — ✅ DONE (2026-08).** Per-player skin picker (Charcoal / Red-felt / Green-felt) persisted via `PUT /api/auth/rummy-theme` + `localStorage`. Fixed a bug where the post-round summary modal re-opened on every 1.5s poll (unstable `round.result` object in a `useEffect` dep) — now gated per `round.id` via a `summaryShownFor` ref, so it no longer blocks the header/theme picker. Added a purely-decorative **ambient background** (`components/RummyAmbiance.jsx`): low-opacity skyline silhouette, slow-drifting glow pools, soft light rays, and gently floating gold specks — all `pointer-events:none`, colour follows the active theme via `--r-gold`, respects `prefers-reduced-motion`. "Elegant Vegas lounge at night" mood, no figures/mascots; does not obstruct cards or the live meld-assist UI.
- **Rummy Low-Balance Alert — ✅ DONE (2026-08, screenshot-verified).** Cash tables only. Trigger mirrors the server's real escrow (`rummy_engine`: `MAX_POINTS(80) × point_value` per seat). Shows a pulsing warning banner (`data-testid=rummy-low-balance`) with a one-tap **Recharge** button that opens the existing `AddCoins` top-up sheet in-table; a recharge CTA is also injected into the post-round summary; on a fresh/waiting table it auto-opens the sheet once (guarded so it never stacks over the summary modal); `doStart` blocks + opens recharge if funds are short. Wallet refreshes on sheet close.
- **Rummy Ambient Music — ✅ DONE (2026-08).** `components/RummyMusic.jsx` — soft, elegant lounge pad synthesized live via Web Audio (Amaj9 through a low-pass with slow swell + filter drift). No lyrics/beat, no external asset (inherently CC0). Header toggle (`data-testid=rummy-music-toggle`), OFF by default, persisted in `localStorage['royal11_rummy_music']`, autoplay-safe (starts on tap).
- **Independence Day (15 Aug) Festive Theme — ✅ DONE (2026-08-14, screenshot-verified).** Standalone visual+audio theme (Festival Gift bonus backend not built yet). `lib/festive.js` `isIndependenceWindow()` gates it to Aug 13–16 (override `?festive=1/0`). `IndependenceBanner.jsx` on Home (dismissible/day) blends tricolor into the dark/gold luxury look: a **continuously wind-fluttering** tricolor flag (`IndependenceFlag.jsx`, CSS `.id-flag-cloth` skew-wave loop — never spins) with a 24-spoke Ashoka Chakra, gradient "Happy Independence Day" title, floating doves, tricolor **fireworks/confetti** canvas (`IndependenceConfetti.jsx`, auto-fades, `prefers-reduced-motion` aware), and a toggleable **original** patriotic instrumental (`IndependenceMusic.jsx`, Web Audio, NOT the anthem, no lyrics, OFF by default). A thin app-wide tricolor ribbon sits at the top of the shell. **Compliance:** strictly generic symbols — NO real people/names (Gandhi/freedom fighters excluded per Emblems & Names Act 1950 + reputational risk); does not use the national anthem melody.

### Proposals — ✅ ALL THREE BUILT & VERIFIED (2026-08-14)
- **A) Support/Complaints + `SUPPORT_HELPER` role — ✅ DONE.** `support_tickets` + `support_ticket_messages`; auto-routes to the player's assigned Admin; manual Escalate walks Admin→Manager→Zonal→Super Admin; internal notes hidden from players. New non-financial `SUPPORT_HELPER` role (added to Role enum + frontend CONSOLE_ROLES) parented to an Admin — verified 403 on every financial/admin endpoint. Backend `support_service.py` + `routers/support.py`. Frontend: console `SupportPanel` (queue+stats+filters+thread+reply/internal/status/escalate) & `SupportTeamPanel` (Admin creates/disables helpers); player `SupportPlayer` drawer (Wallet → Help & Support). Verified via curl e2e + screenshots.
- **B) Referral Program — ✅ DONE.** Both referrer + referee rewarded via the non-withdrawable bonus rail (playthrough). Default "qualified referrer": referee paid at activation, referrer paid on referee's FIRST_RECHARGE/FIRST_WAGER (hooks in `deposit_service.confirm_deposit` + `bonus_service.record_wager`); SA-switchable to SIGNUP. `referral_service.py` + `routers/referrals.py`; SA `referral_config` in RewardsConfigPanel; `referrals` collection JOINED→REWARDED. Frontend: AuthPage `?ref=` prefill + `ReferAndEarn` drawer (code, share link, WhatsApp/Copy/Share, My Referrals). Old direct-credit `_credit_referrer` removed. Verified via curl e2e + screenshots.
- **C) Player payout/contact details = Super-Admin-ONLY — ✅ DONE.** New player-owned `player_profiles` (mobile, bank, UPI, per-channel marketing consent default OFF, consent timestamp+source). Self-service `GET/PUT /api/me/profile`. SA-only `GET /api/admin/players/{id}/sensitive` (audit-logged `PLAYER_SENSITIVE_VIEWED`) + masked `GET /api/admin/players/lookup`. **Verified 403 for ADMIN/MANAGER/other-PLAYER** — strictly SA-only, no lower-role endpoint exposes these fields. `player_profile_service.py` + `routers/profile.py`. Frontend: player `ProfilePayout` drawer (Wallet) + SA console `PlayerPayoutPanel` (masked list + reveal-with-audit). Verified via curl access-matrix + screenshots.

### Proposals PENDING USER REVIEW (2026-08, architecture-only, NOT built)
- **A) Support/Complaints tickets + `SUPPORT_HELPER` sub-role.** `support_tickets` + `support_ticket_messages`; default-route to player's assigned Admin, manual/SLA escalation Admin→Manager→Zonal→SA; new non-financial Admin-parented role (allowlist: view/respond/resolve tickets in scope; denied deposits/balances/config/commissions/user-creation); Console "Support" panel + Admin "Support Team" tab; player "Help/My Tickets".
- **B) Referral Program (extend partial impl in `auth.py`).** Move BOTH referrer+referee rewards to `bonus_service` (non-withdrawable, playthrough); recommended **qualified-referrer** trigger (referrer paid after referee's first recharge/wager ≥ SA threshold — anti-fraud); SA `referral_config`; `referrals` tracking (JOINED→QUALIFIED→REWARDED); player "Refer & Earn" (code + `?ref=` link, WhatsApp/copy share, "My Referrals" list).
- **C) Player contact/payout details = Super-Admin-ONLY.** New player-owned `player_profile` (mobile, bank/UPI, per-channel marketing consent default OFF) as single source of truth (player edits own via `/api/me/profile`); read of sensitive fields gated to SUPER_ADMIN only via `require_roles` + server-side projection strips them for Admin/Manager/Zonal/Support Helper; masked-by-default + reveal-with-audit (`log_action`); India DND/consent model (opt-in, timestamp+source, unsubscribe); actual SMS/WhatsApp sender is a later integration. NOTE: players currently store NO mobile/bank/UPI/consent — this introduces it (pairs with future Withdraw feature).
- **Tournaments** — reuse the per-round provably-fair engine, orchestrated across many rounds. Recommended MVP = **points-accumulation** (register → play a series of hands → cumulative tournament points → leaderboard → top-N split prize pool) over knockout brackets (easier to schedule with live humans). New: `tournaments` + `tournament_entries` collections; entry fee → prize pool, rake→split reused; idempotent `tourney_payout:{id}:{user}`. **Sequencing: Phase 1.5** (after core Rummy cash tables proven); Phase 1 just wires the existing "Tournament" lobby tab to a registration/"coming soon" shell.
- **Free Practice mode — ✅ DONE (2026-08, pytest test_casino.py 7/7 + UI verified)**. `app/games/practice_service.py` (separate practice-chip balance, start 10k / auto-refill to 5k when <500, never touches real wallet). Engine `is_practice` flag on tables/rounds: practice rounds use chips, skip rake/revenue/XP, but use the SAME provably-fair RNG. Frontend Cash/Practice lobby toggle + practice-balance chip + "Practice" tags (`pages/CasinoPage.jsx`). Applies to all casino games. `GET /api/casino/practice/balance`.
- **VIP / Level progression — ✅ FOUNDATION DONE (2026-08, pytest 7/7 + UI verified)**. `app/games/progression_service.py` (idempotent `add_wager_xp` via `xp_events`, tiers Bronze/Silver/Gold/Platinum/Royal at 0/5k/25k/100k/500k XP, rakeback 0/2/5/8/12%, default 1 XP per 10 coins, practice 0×; all in `vip_config`, Super-Admin-tunable). XP accrues on every real-money casino wager from day one. `GET /api/casino/progression/me`; SA `GET/PUT /api/casino/admin/vip-config`. Frontend tier badge + XP bar in Card Games header. TODO (Phase L): hook Fantasy entries into XP; build full rewards/rakeback-payout + celebration UI.
- **Tournaments** — reuse the per-round provably-fair engine, orchestrated across many rounds. Recommended MVP = **points-accumulation**. New: `tournaments` + `tournament_entries`. **Sequencing: Phase 1.5**; Phase 1 wires the "Tournament" lobby tab to a shell. NOT built yet.
- ⚠️ **REQUIRED PRE-LAUNCH (deferred by user, do NOT skip before production):** (1) responsible-gaming guardrails — per-player daily loss/spend caps; (2) state-restriction gating toggle (AP/Telangana/TN/Assam for Rummy; broader for chance games — Goa/Sikkim/Daman only). (3) Re-surface the legal/compliance risk to the user before any production launch (chance games are high legal risk in most of India). (4) WebSocket + Mongo Change Streams upgrade for real-time (currently polling).

## Notes
- **Q JOKER → renamed "Shalu" + 6-language chat + voice — ✅ DONE (2026-08-15).** All player-facing text now says **Shalu** (widget header, greeting, placeholder, alt text, escalation subject "Escalated from Shalu chat", backend persona/transcript). Internal `JOKER` const + code vars kept. Mascot art unchanged.
  - **Language selector** (chatbot-only): 6 langs (en/hi/ta/te/bn/mr) picker in header (`chatbot-lang-btn`/`chatbot-lang-menu`/`chatbot-lang-{code}`), persisted `localStorage royal11_chat_lang` (default = browser locale if in set else en). Sent as `language` to `POST /api/chatbot/message` (validated regex); Gemini instructed to reply in-language (keeps "ROYAL 11" etc.). Quick chips translated per language. Verified in-language replies via curl (EN/HI/TE). Rest of app stays English (out of scope).
  - **Voice** (chatbot-only, browser-native Web Speech API): mic input `chatbot-mic` (SpeechRecognition, lang=selected BCP-47) transcribes → sends as text; voice output toggle `chatbot-tts-toggle` (SpeechSynthesis) reads replies aloud, persisted `royal11_chat_tts`. **COVERAGE CAVEAT:** STT is Chrome/Edge-only; TTS voice availability for ta/te/bn/mr depends on the user's OS/browser (English & Hindi broadly supported on Chrome/Android; other 4 are best-effort, may be silent where the OS lacks the voice). This is a player↔AI-support feature only — NOT player↔player voice (does not touch the anti-collusion no-voice-chat rule).
- **Rummy UI pass + Daily Bonus + Referral 200 + Joker/Shalu mascot — ✅ DONE (2026-08-15/16).**
  - **Daily Bonus** (NEW, server-authoritative): `daily_bonus_service.py` + `routers/daily_bonus.py`. `GET /api/daily-bonus/status` → `{enabled, amount, claimable, claimed_today, last_claim_at, next_claim_at, server_time}`; `POST /api/daily-bonus/claim` → idempotent one-claim-per **IST calendar day** (`daily_bonus:{uid}:{YYYY-MM-DD}`), credited via non-withdrawable bonus rail. SA config `GET|PUT /api/daily-bonus/config {enabled, amount}` (default 50). Frontend `components/DailyBonusWidget.jsx` = persistent HUD (server-corrected countdown, CLAIM), mounted bottom-left on the Rummy table. Test: `tests/test_daily_bonus.py`.
  - **Referral reward** now **flat 200 to referrer / 75 to referee** (`referral_service.py` DEFAULT + live `referral_config` updated). Rewards modal `components/ReferAndEarn.jsx` heading shows "Refer & Earn 200 Coins", added **promo-code apply** box (`POST /api/promo/apply`) + disclaimer "Virtual coins only · No cash value · No withdrawal or redemption".
  - **Rummy table** (`pages/RummyTable.jsx`): added header **Rewards** button (opens ReferAndEarn), **EMOJI ONLY** badge bottom-right, action bar recolored (Declare=gold, Discard=crimson). Full AAA photoreal room + My-Table customization screen + 52-card "V2" photoreal deck = **phase 2** (not built this pass).
  - **Unified "Royal Mascot Joker"** (generated on-spec, `lib/casinoAssets.js` → `ROYAL_JOKER_CARD` + `ROYAL_JOKER_AVATAR`): replaces the in-game Joker playing card (`components/PlayingCard.jsx` joker branch → image) AND the Q JOKER chatbot avatar (`components/SupportChatbot.jsx` widget + header). Chatbot backend/logic untouched.
- **Auth UX + password reset — ✅ DONE (2026-08-15, iter 35 100%).**
- **Crypto USDT on-chain auto-approve — ✅ DONE (2026-08-15, curl-verified vs live TronScan).** On Admin USDT purchase submit, `tron_service.py` verifies the `tx_id` on TronScan (public, no-key; USDT TRC-20 contract TR7NHqjeKQxGTCi8q8ZY4pL8otSzgjLj6t) — confirmed transfer, amount match (0.5% tol), receiving address == SA `usdt_address`, tx_id not already credited. All pass → auto-confirm+credit (`decided_by=SYSTEM_AUTO_CHAIN`, `auto_approved=true`, `chain_verification` verdict stored); else manual. SA-configurable cap `auto_approve_max_usdt` (0/null=unlimited). Brief retry for chain-confirmation lag. Frontend: SA panel cap field + "Auto-approved" filter + chain-verified badge/detail panel.
- **Deposit OCR auto-approve — ✅ DONE (2026-08-15, pytest `tests/test_deposit_auto_approve.py` + config curl-verified).** Per-Admin opt-in (`/api/admin/deposits/auto-approve-config`, default OFF). On player deposit create: auto-confirm+credit only when OCR overall=="match" AND not duplicate_utr AND amount≤₹5,000 (hard cap, non-configurable) AND admin opted-in; else manual. Auto rows: `confirmed_by=SYSTEM_AUTO_OCR`, `auto_approved=true`. Console → Deposits: toggle + All/Pending/Auto-approved tabs + badge.
- **Player UPI QR upload — ✅ DONE (2026-08-15, curl + UI verified).** Optional data-capture field on the Player Profile & Payout form (below UPI ID), mirroring the SA crypto-QR pattern (text UPI field + image upload together). Endpoints (PLAYER): `POST /api/me/profile/upi-qr` (multipart `file`, image-only, ≤5MB), `GET /api/me/profile/upi-qr` (streams own QR), `DELETE /api/me/profile/upi-qr`. Profile GET now returns `has_upi_qr` bool (raw `upi_qr_path` never exposed). SUPER_ADMIN-only view: `GET /api/admin/players/{id}/upi-qr` (Admin/Manager/etc → 403). Stored via `storage_service` under `royal11/player_profiles/{uid}/upi_qr`. NOT a payout/withdrawal mechanism — on-file record only; no money-movement logic changed. Files: `player_profile_service.py` (set/get/clear_upi_qr), `routers/profile.py`, `components/ProfilePayout.jsx` (dropzone + blob-fetch preview + replace/remove).
- **Bank/Profile client-side validation — ✅ DONE (2026-08-15, testing agent iter 34, frontend 100%).** Backend `models.py` enforces mobile = exactly 10 digits (`_validate_mobile`) and IFSC `^[A-Z]{4}0[A-Z0-9]{6}$` (`_validate_ifsc`) on `BankAccountInput` (Admin) and `PlayerProfileInput` (Player). Matching client-side validation added to: Player `components/ProfilePayout.jsx` (10-digit-only mobile with fixed +91 prefix, IFSC auto-uppercase+regex+live Razorpay lookup auto-filling bank name, UPI VPA format check `^[\w.\-]{2,256}@[a-zA-Z]{2,64}$` — format-only, no live payee verification, Save disabled on any invalid input) and Admin `console/BankAccountPanel.jsx` (IFSC auto-uppercase+regex+Razorpay lookup, Submit disabled on invalid). Prevents 422 errors on both forms. Live IFSC lookup uses public `https://ifsc.razorpay.com/{code}` (no auth).
- **App-wide `ErrorBoundary` — ✅ ADDED (2026-08-14).** `components/ErrorBoundary.jsx` wraps the whole tree in `App.js`. Previously NO error boundary existed, so any uncaught render error blanked the entire app (white screen). Now shows a graceful "Something went wrong — Reload" screen + prints the real error message for diagnosis. Also hardened `ScrollTop` effect to return nothing. NOTE: a reported production blank-screen/`ScrollTop` error could NOT be reproduced on current code in dev OR a fresh prod build across all routes+deep-links — most likely a stale/mid-deploy bundle hitting the (now-fixed) missing safety net. **Requires a redeploy to take effect in production.**
- Frontend now server-authoritative for Auth + Wallet (JWT Bearer in `localStorage['royal11_token']`).
- Coin-spending UI actions are intentionally disabled ("Coming soon") until their ledger endpoints exist.


## 2026-08-15 — Later session (fork)
- **Chatbot renamed "Shalu" → "Zoya" — ✅ DONE (curl-verified persona).** All 13 refs swapped across `components/SupportChatbot.jsx` (header title, avatar alt, greeting, input placeholder, escalation subject), `app/chatbot_service.py` (Gemini persona system prompt), `app/routers/chatbot.py` (ticket escalation subject). Live chat replies now self-identify as "Zoya".
- **Realistic V2 deck (full 52 faces) — ✅ DONE (visual-verified full 54-card render + zoomed pip-count check).** Upgraded the shared `components/PlayingCard.jsx` renderer (used by Rummy + High Card + Casino) from a single-center-symbol flat look to an **authentic deterministic premium deck**: traditional multi-pip layouts for 2–10 (correct counts/positions, bottom-half pips rotated 180°), ornate single-pip Aces, and gold court-frame treatments for J/Q/K (crown ornament + large rank + suit). New CSS in `pages/casino-vegas.css` (`.pc-pipgrid/.pc-pipdot/.pc-ace/.pc-court*`, gold hairline `.pc-face::after`, richer ivory material). **Chose deterministic vector rendering over 52 AI images** — AI cannot reliably render correct pip counts/ranks, and a mislabeled card would break Rummy scoring. Card BACK + Joker remain the photoreal V2 image assets; deck is now fully visually consistent. Game logic untouched (rendering-only change).
- **Daily Bonus 7-day streak (Day-7 jackpot) — ✅ DONE (pytest `tests/test_daily_bonus.py` + curl-verified + widget screenshot).** Escalating ladder: base amount on days 1–6, bigger jackpot on day 7, then loops back to day 1. Streak continues on unbroken IST-day claims and resets to day 1 on any missed calendar day. `daily_bonus_service.py`: added `STREAK_LEN=7`, `day7_amount` config (default 250), `_ladder()`, `_next_streak_day()` (continue/loop/reset), `_ist_today_yesterday()`; each claim now stores `streak_day`; `status()` returns `ladder`, `streak_day`, `streak_len`, `base_amount`, `day7_amount` (`amount` kept = current-day reward for UI back-compat; pre-existing claim rows without `streak_day` default to 1). SA config PUT accepts `day7_amount` (`routers/daily_bonus.py`). Frontend `DailyBonusWidget.jsx`: 7-dot streak ladder (`daily-bonus-streak`, `streak-day-{n}`), Day-7 crown accent, day/amount label, jackpot toast. Test harness note: DB streak scenarios run inside the existing single-loop matrix test (via `_streak_scenarios()` helper) to avoid the known shared-motor-client "event loop closed" flakiness from multiple `asyncio.run` calls.


- **Pool + Deals Rummy variants (6-seat) — ✅ DONE (pytest `tests/test_rummy_match.py` + testing_agent iter38 100% backend & frontend + e2e curl).** New game types `rummy_pool` (config `pool_type` 101|201) and `rummy_deals` (config `num_deals` 2|3), both min 2 / max 6, `has_turns`. A multi-deal MATCH layer sits on top of the shared single-deal turn engine (`games/rummy_engine.py`): `_start_match()` charges the fixed `entry_fee` ONCE at match start (idempotent), seeds `table.match` (variant, prize_pool = entries − rake, scores, participants); `_settle()` dispatches to `_settle_points` (unchanged) vs `_settle_deal_multi` (no per-deal coin move — deal points accumulate on `match.scores`); Pool eliminates a player at `score >= pool_limit` (last player standing wins), Deals runs `num_deals` deals (lowest total wins); `_start_next_deal()` deals the next deal to remaining/all participants; `_end_match()` pays the prize pool once + records match rake (`round_id=match:{id}`). `get_state()` now returns a public `match` view (`_match_public`) and `round.config.variant`. Points Rummy behaviour is UNCHANGED (regression-verified). Config normalized in `games/engine.py create_table`; catalog entries in `games/catalog.py`. Frontend `CasinoPage.jsx`: 3 rummy lobby tabs + Pool/Deals config panel (`rummy-variant-config`, `pool-type-*`, `num-deals-*`, `entry-fee-*`) + variant-aware table cards. `RummyTable.jsx`: match HUD (`rummy-match-hud`, `match-score-{uid}`, `rummy-match-over`), variant-aware header/labels, once-per-match low-balance logic.
- **AAA Rummy table (premium visual baseline, all breakpoints) + mobile usability — ✅ DONE (screenshots at 390 / 820 / 1440).** RummyTable is now a full-screen immersive overlay (`fixed inset-0 z-[70]`) so the app SideNav/BottomNav never eat table space on phone/tablet (fixes the "sidebar too wide" complaint); header controls collapse to icons on <sm; the floating chatbot launcher is hidden while a table is open (`body.rummy-immersive` CSS); the Daily-Bonus/emoji HUD is hidden during play so it never covers the action bar. Same premium look at every breakpoint (responsively scaled, not a cheaper mobile fallback): ornate room backdrop with a baked-in host (host-visible `AAA_ROOM_HOST` generated image vs host-hidden `AAA_ROOM_BG`, toggled by `rummy-host-toggle`, persisted `royal11_rummy_host`; felt nudged right + backdrop left-biased responsively so the host stays visible beside the table at all sizes), elliptical gold-trimmed crimson felt (`.vegas-felt--aaa`, consistent ellipse on mobile), labeled piles DRAW/DISCARD/JOKER (`.pile-label`), and a YOUR-TURN / opponent turn indicator (`.turn-indicator`, `rummy-turn-indicator`). Presentation-only — game logic / RNG / ledger / turn mechanics untouched.
